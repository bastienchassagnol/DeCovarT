% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_06_decovart_inference.R
\name{multistart_decovart}
\alias{multistart_decovart}
\title{Refit DeCovarT from several random starts to probe multimodality}
\usage{
multistart_decovart(
  y,
  mean_signature_matrix,
  Sigma,
  n_starts = 5L,
  solver = deconvolute_ratios_Marquardt_Levenberg,
  loglik_tol = 1e-04,
  ...
)
}
\arguments{
\item{y}{Numeric vector (or one-column matrix)
\eqn{\boldsymbol{y}\in\mathbb{R}^{G}}.}

\item{mean_signature_matrix}{Numeric matrix
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (plug-in means).}

\item{Sigma}{Array of cell-type covariances in
\eqn{\mathcal{M}_{G\times G\times J}}.}

\item{n_starts}{Number of random starts (in addition to the
equi-balanced start).}

\item{solver}{Solver accepting \code{initial_p} and \code{return_model};
defaults to \code{\link[=deconvolute_ratios_Marquardt_Levenberg]{deconvolute_ratios_Marquardt_Levenberg()}}.}

\item{loglik_tol}{Two starts count as reaching the same mode when their
log-likelihoods differ by less than this amount.}

\item{...}{Passed to \code{solver} (e.g. \code{epsilon}, \code{itmax}).}
}
\value{
A list with \code{coefficients} (best fit), \code{loglik},
\code{loglik_range}, \code{starts} (matrix of per-start estimates),
\code{logliks}, and \code{multimodal}.
}
\description{
The realised DeCovarT log-likelihood is not globally concave, so a
successful convergence code, a small relative-distance-to-minimum and a
negative-definite local Hessian together establish a \strong{local} maximum
only. Refitting from independent Dirichlet\eqn{(1,\ldots,1)} starts and
comparing the attained log-likelihoods is the cheapest direct probe of
a second, equally good or better mode.
}
\examples{
mu <- matrix(c(20, 40, 15, 40, 20, 25), nrow = 3)
colnames(mu) <- paste0("ct", 1:2)
Sigma <- array(c(diag(3), diag(3)), dim = c(3, 3, 2))
y <- drop(mu \%*\% c(0.6, 0.4))
set.seed(1)
multistart_decovart(y, mu, Sigma, n_starts = 3L)$multimodal
}
\seealso{
\code{\link[=boundary_diagnostics]{boundary_diagnostics()}}, \code{\link[=fit_decovart]{fit_decovart()}}

Other decovart_inference: 
\code{\link{bootstrap_decovart}()},
\code{\link{boundary_diagnostics}()},
\code{\link{chi_bar_square_pvalue}()},
\code{\link{confint_profile_decovart}()},
\code{\link{equivariance_check_decovart}()},
\code{\link{lrt_decovart}()},
\code{\link{profile_loglik_decovart}()},
\code{\link{reference_bootstrap_decovart}()},
\code{\link{restricted_mle_decovart}()}
}
\concept{decovart_inference}
