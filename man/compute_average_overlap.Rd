% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_metrics.R
\name{compute_average_overlap}
\alias{compute_average_overlap}
\title{Average pairwise overlap of a Gaussian mixture}
\usage{
compute_average_overlap(true_theta, k = length(true_theta$p))
}
\arguments{
\item{true_theta}{List with \code{p} (length \eqn{J}), \code{mu} (\eqn{G\times J}
mean matrix) and \code{sigma} (\eqn{G\times G\times J} covariance array),
as in a GMM
\eqn{(\boldsymbol{p},\boldsymbol{\mu},\{\boldsymbol{\Sigma}_j\})}.}

\item{k}{Number of components \eqn{J} (kept for compatibility;
MixSim uses \code{length(true_theta$p)}).}
}
\value{
Scalar average pairwise overlap (MixSim \code{BarOmega}).
}
\description{
Returns MixSim's \strong{BarOmega}: the unweighted mean of pairwise
overlaps
\deqn{
  \overline{\omega}
  =
  \frac{2}{J(J-1)}
  \sum_{1\le j<\ell\le J}
  \bigl(\Omega_{j\ell}+\Omega_{\ell j}\bigr)
  \in[0,1]
}
(up to the MixSim numerical convention), where
\eqn{\Omega_{j\ell}=\Pr_{X\sim f_j}(X\text{ classified as }\ell)}
already uses the mixture weights \eqn{\boldsymbol{p}} inside the
Bayes / MAP rule of \code{\link[MixSim:overlap]{MixSim::overlap()}}. Do \strong{not} multiply the
directional masses by \eqn{p_j} again.
}
\examples{
set.seed(1)
theta <- list(
  p = c(0.5, 0.5),
  mu = cbind(c(0, 0), c(3, 0)),
  sigma = array(c(diag(2), diag(2)), dim = c(2, 2, 2))
)
compute_average_overlap(theta)
}
