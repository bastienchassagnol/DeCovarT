% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_02_benchmark_visualisation.R
\name{plot_mc_raincloud}
\alias{plot_mc_raincloud}
\title{Horizontal raincloud of Monte Carlo proportion estimates}
\usage{
plot_mc_raincloud(
  benchmark,
  quantity = c("error", "estimate"),
  facet_rows = NULL,
  facet_cols = NULL,
  .width = c(0.5, 0.95)
)
}
\arguments{
\item{benchmark}{List from \code{\link[=run_simulation_benchmark]{run_simulation_benchmark()}}, or a table
from \code{\link[=pivot_mc_estimates]{pivot_mc_estimates()}}.}

\item{quantity}{\code{"error"} (default) or \code{"estimate"}.}

\item{facet_rows, facet_cols}{Optional column names for
\code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}} rows and columns.}

\item{.width}{Passed to \code{\link[ggdist:stat_halfeye]{ggdist::stat_halfeye()}}; default
\code{c(0.5, 0.95)}.}
}
\value{
A \code{ggplot} object.
}
\description{
Half-eye densities, dots, and empirical 50\% / 95\% intervals
\insertCite{allenRaincloudPlotsMultiplatform2019}{DeCovarT} for the
Monte Carlo sampling distribution of \eqn{\hat p_j}. Default
aesthetics: numeric axis is the estimation error
\eqn{\hat p_j-p_j^{\star}} (so a vertical line at 0 is bias);
the y-axis is the cell type; colour and dodge group the deconvolution
algorithm. Optional \code{facet_rows} / \code{facet_cols} split scenarios
(for example number of genes versus pairwise cosine).
}
\details{
The inner interval is the central 50\% of Monte Carlo replicates; the
outer interval is the central 95\%. These are \strong{not} confidence
intervals for \eqn{p_j}.
}
\examples{
\dontshow{if (.Platform$OS.type != "windows") withAutoprint(\{ # examplesIf}
set.seed(1)
genes <- paste0("g", 1:2)
cts <- paste0("ct", 1:2)
mu <- matrix(c(20, 22, 22, 20), nrow = 2, dimnames = list(genes, cts))
Sigma <- array(
  c(1, 0, 0, 1, 1, 0, 0, 1),
  dim = c(2, 2, 2),
  dimnames = list(genes, genes, cts)
)
out <- run_simulation_benchmark(
  tibble::tibble(
    n_genes = 2L,
    cosine = 0,
    true_theta = list(list(p = c(0.5, 0.5), mu = mu, sigma = Sigma))
  ),
  deconvolution_functions = list(
    "nnls" = list(FUN = deconvolute_ratios_nnls)
  ),
  n = 4,
  cores = 1
)
if (requireNamespace("ggdist", quietly = TRUE)) {
  plot_mc_raincloud(out, facet_cols = "cosine")
}
\dontshow{\}) # examplesIf}
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=plot_mc_forest]{plot_mc_forest()}}, \code{\link[=pivot_mc_estimates]{pivot_mc_estimates()}}
}
