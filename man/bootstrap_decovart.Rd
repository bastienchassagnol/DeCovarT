% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_06_decovart_inference.R
\name{bootstrap_decovart}
\alias{bootstrap_decovart}
\title{Parametric bootstrap for DeCovarT proportions and boundary tests}
\usage{
bootstrap_decovart(
  y = NULL,
  mean_signature_matrix,
  Sigma,
  bulk_expression = NULL,
  p = NULL,
  null_value = NULL,
  n_boot = 500L,
  level = 0.95,
  n_replicates = NULL,
  epsilon = 10^-8,
  itmax = 500L
)
}
\arguments{
\item{y}{Numeric vector (or one-column matrix)
\eqn{\boldsymbol{y}\in\mathbb{R}^{G}}.}

\item{mean_signature_matrix}{Numeric matrix
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (plug-in means).}

\item{Sigma}{Array of cell-type covariances in
\eqn{\mathcal{M}_{G\times G\times J}}.}

\item{bulk_expression}{Numeric matrix \eqn{\boldsymbol{Y}\in
  \mathcal{M}_{G\times N}} of replicate bulk profiles sharing one
composition, used instead of \code{y} for the pooled (population) test.}

\item{p}{Composition to simulate from. Defaults to the MLE computed
from \code{y} / \code{bulk_expression}.}

\item{null_value}{Named numeric vector of hypothesised ratios, e.g.
\code{c(celltype_2 = 0)}.}

\item{n_boot}{Number of bootstrap replicates.}

\item{level}{Confidence level for percentile intervals.}

\item{n_replicates}{Number of bulk columns simulated per replicate;
defaults to the number of observed columns.}

\item{epsilon, itmax}{Relative convergence tolerance and iteration
budget passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A list with \code{estimates} (\eqn{J\times} \code{n_boot} matrix),
\code{interval} (percentile bounds), \code{p_simulated}, and, when
\code{null_value} is supplied, \code{statistic} (observed \eqn{D}),
\code{null_statistics}, and \code{p_value}.
}
\description{
The conditional law of the bulk profile is fully specified, so the
asymptotic calibrations of \code{\link[=lrt_decovart]{lrt_decovart()}} can be replaced by
simulation. Given plug-in moments and a composition
\eqn{\boldsymbol{p}}, the function draws
\eqn{\boldsymbol{Y}^{(b)}\sim
\mathcal{N}_{G}(\boldsymbol{\mu}\boldsymbol{p},
\boldsymbol{\Sigma}(\boldsymbol{p}))}, refits DeCovarT, and returns the
empirical distribution of \eqn{\hat{\boldsymbol{p}}^{(b)}} together
with percentile intervals.
}
\details{
Supplying \code{null_value} switches to a restricted bootstrap: replicates
are generated under the restricted MLE and the likelihood-ratio
statistic is recomputed on each, giving a Monte Carlo p-value that does
not rely on the \eqn{50{:}50} asymptotic weights. For a composite null
the remaining nuisance proportions are profiled at their restricted
estimate, so the procedure is a plug-in (approximate) rather than an
exact bootstrap.

Reference uncertainty is \strong{not} propagated here: \eqn{\boldsymbol{\mu}}
and \eqn{\boldsymbol{\Sigma}_j} are treated as known, exactly as in the
frequentist likelihood. To resample the purified observations that
produced those plug-in moments, use
\code{\link[=reference_bootstrap_decovart]{reference_bootstrap_decovart()}}.
}
\examples{
mu <- matrix(c(20, 40, 15, 40, 20, 25), nrow = 3)
colnames(mu) <- paste0("ct", 1:2)
Sigma <- array(c(diag(3), diag(3)), dim = c(3, 3, 2))
y <- drop(mu \%*\% c(0.6, 0.4))
set.seed(1)
bootstrap_decovart(y, mu, Sigma, n_boot = 20)$interval
}
\seealso{
\code{\link[=lrt_decovart]{lrt_decovart()}}, \code{\link[=chi_bar_square_pvalue]{chi_bar_square_pvalue()}},
\code{\link[=reference_bootstrap_decovart]{reference_bootstrap_decovart()}}

Other decovart_inference: 
\code{\link{boundary_diagnostics}()},
\code{\link{chi_bar_square_pvalue}()},
\code{\link{confint_profile_decovart}()},
\code{\link{equivariance_check_decovart}()},
\code{\link{lrt_decovart}()},
\code{\link{multistart_decovart}()},
\code{\link{profile_loglik_decovart}()},
\code{\link{reference_bootstrap_decovart}()},
\code{\link{restricted_mle_decovart}()}
}
\concept{decovart_inference}
