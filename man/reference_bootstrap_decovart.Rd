% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_06_decovart_inference.R
\name{reference_bootstrap_decovart}
\alias{reference_bootstrap_decovart}
\title{Reference-based bootstrap for signature and composition uncertainty}
\usage{
reference_bootstrap_decovart(
  y = NULL,
  reference_profiles,
  bulk_expression = NULL,
  cell_type_labels = NULL,
  donor_ids = NULL,
  method = c("donors", "cells", "dirichlet"),
  n_boot = 199L,
  level = 0.95,
  dirichlet_alpha = 1,
  regenerate_bulk = FALSE,
  p = NULL,
  n_replicates = NULL,
  ridge = 1e-06,
  epsilon = 10^-8,
  itmax = 500L
)
}
\arguments{
\item{y}{Numeric vector (or one-column matrix)
\eqn{\boldsymbol{y}\in\mathbb{R}^{G}}.}

\item{reference_profiles}{Named list of \eqn{G\times n_j} matrices,
one purified sample matrix per cell type. Names must match the
intended column order of the signature. Alternatively, a single
\eqn{G\times n_{\mathrm{ref}}} matrix together with
\code{cell_type_labels}.}

\item{bulk_expression}{Numeric matrix \eqn{\boldsymbol{Y}\in
  \mathcal{M}_{G\times N}} of replicate bulk profiles sharing one
composition, used instead of \code{y} for the pooled (population) test.}

\item{cell_type_labels}{Optional character or factor vector of length
\code{ncol(reference_profiles)} when \code{reference_profiles} is a matrix.}

\item{donor_ids}{Donor (or other clustering) labels aligned with the
purified columns. A named list of vectors matching
\code{reference_profiles}, or a vector of length
\code{ncol(reference_profiles)} when the reference is a single matrix.
Required for \code{method = "donors"}.}

\item{method}{One of \code{"donors"} (default), \code{"cells"}, or
\code{"dirichlet"}.}

\item{n_boot}{Number of bootstrap replicates.}

\item{level}{Confidence level for percentile intervals.}

\item{dirichlet_alpha}{Positive Dirichlet concentration: a scalar
recycled to \eqn{J}, or a length-\eqn{J} vector. Ignored unless
\code{method = "dirichlet"}. The default \code{1} is uniform on the simplex.}

\item{regenerate_bulk}{If \code{TRUE} (\code{donors} / \code{cells} only), simulate
a new bulk from the bootstrapped moments instead of keeping the
observed \eqn{\boldsymbol{Y}} fixed.}

\item{p}{Composition to simulate from. Defaults to the MLE computed
from \code{y} / \code{bulk_expression}.}

\item{n_replicates}{Number of bulk columns simulated per replicate;
defaults to the number of observed columns.}

\item{ridge}{Non-negative ridge multiple for the sample covariance of
each cell type. The default \code{1e-6} is relative to the mean diagonal.}

\item{epsilon, itmax}{Relative convergence tolerance and iteration
budget passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A list with \code{method}, \code{estimates} (\eqn{J\times} \code{n_boot}),
\code{interval} (percentile bounds), \code{mean_signature_matrix} / \code{Sigma}
from the original reference, and \code{p_hat} from the original plug-in
fit when a bulk is supplied. For \code{method = "dirichlet"},
\code{p_simulated} stores the drawn compositions.
}
\description{
The frequentist DeCovarT likelihood treats
\eqn{\boldsymbol{\mu}} and each \eqn{\boldsymbol{\Sigma}_j} as known.
In a labelled, reference-based model those moments come from purified
profiles whose \emph{cell-type names stay attached}. This bootstrap
resamples the experimental units that generated the reference (donors,
by default), or redraws compositions from a Dirichlet law, then
re-estimates the plug-in moments and refits
\insertCite{efronBootstrapMethodsAnother1979}{DeCovarT}.
}
\details{
Permuting gene order, or permuting cell-type labels of an already
averaged signature, is \strong{not} a bootstrap: the first destroys the
gene-wise pairing of bulk and reference, and the second is algebraic
equivariance rather than a source of uncertainty (see
\code{\link[=equivariance_check_decovart]{equivariance_check_decovart()}} and
\code{vignette("DeCovarT-MLE-properties")}). The three \code{method} options
instead resample units that actually vary.

\describe{
\item{\code{donors}}{Cluster bootstrap: resample donor identifiers
\emph{within each cell type}, take every purified column of the
sampled donors (a donor drawn twice is included twice), rebuild
\eqn{(\boldsymbol{\mu},\boldsymbol{\Sigma}_j)}, and refit.
This is the default: it targets biological / reference-population
uncertainty rather than technical cell-level noise alone.}
\item{\code{cells}}{Resample purified columns independently within each
cell type (with replacement) and rebuild the plug-in moments.
Use this when donor labels are unavailable.}
\item{\code{dirichlet}}{Hold the original plug-in moments fixed, draw
\eqn{\boldsymbol{p}^{(b)}\sim\mathrm{Dirichlet}(\boldsymbol{\alpha})},
simulate bulk profiles from the Gaussian convolution, and refit.
This is a composition-sweep diagnostic, not a confidence interval
for one observed bulk.}
}

For \code{donors} and \code{cells} the observed bulk is kept fixed unless
\code{regenerate_bulk = TRUE}, in which case each replicate also draws
\eqn{\boldsymbol{Y}^{(b)}} from the bootstrapped moments at \code{p}
(the observed MLE by default). That is the pipeline
reference units \eqn{\to S^{(b)}\to Y^{(b)}\to\hat{\boldsymbol{p}}^{(b)}}.

When a cell type has fewer purified columns than genes, the sample
covariance is singular. A ridge \code{ridge * mean(diag(S))} is then added
on the diagonal so that \eqn{\boldsymbol{\Sigma}(\boldsymbol{p})} remains
factorisable. Set \code{ridge = 0} to disable that guard (and accept
possible Cholesky failures).
}
\examples{
set.seed(1)
mu <- matrix(c(20, 40, 15, 40, 20, 25), nrow = 3)
colnames(mu) <- paste0("ct", 1:2)
Sigma <- array(c(diag(3), diag(3)), dim = c(3, 3, 2))
refs <- lapply(seq_len(2), function(j) {
  t(MASS::mvrnorm(n = 8, mu = mu[, j], Sigma = Sigma[, , j]))
})
names(refs) <- colnames(mu)
donor_ids <- lapply(refs, function(x) {
  rep(paste0("d", 1:2), length.out = ncol(x))
})
y <- drop(mu \%*\% c(0.6, 0.4))
reference_bootstrap_decovart(
  y,
  refs,
  donor_ids = donor_ids,
  n_boot = 15
)$interval
}
\references{
\insertRef{efronBootstrapMethodsAnother1979}{DeCovarT}
}
\seealso{
\code{\link[=bootstrap_decovart]{bootstrap_decovart()}}, \code{\link[=equivariance_check_decovart]{equivariance_check_decovart()}}

Other decovart_inference: 
\code{\link{bootstrap_decovart}()},
\code{\link{boundary_diagnostics}()},
\code{\link{chi_bar_square_pvalue}()},
\code{\link{confint_profile_decovart}()},
\code{\link{equivariance_check_decovart}()},
\code{\link{lrt_decovart}()},
\code{\link{multistart_decovart}()},
\code{\link{profile_loglik_decovart}()},
\code{\link{restricted_mle_decovart}()}
}
\concept{decovart_inference}
