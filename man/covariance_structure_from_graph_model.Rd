% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_02_covariance_backends.R
\name{covariance_structure_from_graph_model}
\alias{covariance_structure_from_graph_model}
\title{Recommend a covariance backend from a network topology}
\usage{
covariance_structure_from_graph_model(graph_model)
}
\arguments{
\item{graph_model}{One of \code{"erdos_renyi"}, \code{"hub"}, \code{"star"},
\code{"scale_free"}, \code{"stochastic_block_model"}, \code{"small_world"}, \code{"band"},
\code{"ar"}. Matching is case-insensitive.}
}
\value{
A single string: one of \code{"dense"}, \code{"block"}, \code{"band"},
\code{"sparse"}, \code{"diag_lowrank"}.
}
\description{
Maps a \code{graph_model} name (see
\code{\link[=generate_random_network_skeleton]{generate_random_network_skeleton()}}) to the covariance-factorisation
\code{structure} most likely to help. This encodes the decision-tree workflow:
the dense Cholesky is always a safe default; the structured backends are
refinements when the covariance (not merely the precision) inherits
exploitable structure.
}
\details{
A sparse gene-network \strong{precision} matrix
\eqn{\boldsymbol{\Omega}_j=\boldsymbol{\Sigma}_j^{-1}} does \strong{not} imply a
sparse \strong{covariance} \eqn{\boldsymbol{\Sigma}_j}; the inverse of a sparse
precision is generally dense. The mapping below therefore expresses
modelling intent, and the returned structure only accelerates the
likelihood when \eqn{\boldsymbol{\Sigma}(\boldsymbol{p})} itself (or a
cheap representation of it) has that structure. Disconnected gene modules
give an exactly block-diagonal covariance; shared regulatory programs give
a diagonal-plus-low-rank covariance; a small set of bridge / housekeeping
hubs gives an arrow pattern handled by the Schur complement.
}
\examples{
covariance_structure_from_graph_model("stochastic_block_model")
covariance_structure_from_graph_model("erdos_renyi")
}
\seealso{
\code{\link[=new_decovart_covariance]{new_decovart_covariance()}}
}
\keyword{internal}
