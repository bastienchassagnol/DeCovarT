% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_01_first_generation_deconvolution.R
\name{deconvolute_ratios_gls}
\alias{deconvolute_ratios_gls}
\title{Generalised least squares with a fixed residual covariance}
\usage{
deconvolute_ratios_gls(y, mean_signature_matrix, W, inverse = TRUE)
}
\arguments{
\item{y}{Numeric bulk vector of length \eqn{G}.}

\item{mean_signature_matrix}{Numeric matrix \eqn{G\times J}.}

\item{W}{Residual covariance (when \code{inverse = TRUE}) or precision
(when \code{inverse = FALSE}), of size \eqn{G\times G}.}

\item{inverse}{Passed to \code{\link[MASS:lm.gls]{MASS::lm.gls()}}; \code{TRUE} means \code{W} is a
covariance matrix.}
}
\value{
Named simplex vector of length \eqn{J}.
}
\description{
Fits
\eqn{\boldsymbol{y}\approx\boldsymbol{\mu}\boldsymbol{p}} by
\code{\link[MASS:lm.gls]{MASS::lm.gls()}} with a \strong{known} \eqn{G\times G} residual
covariance \code{W} that does not depend on \eqn{\boldsymbol{p}}, then
\code{\link[=repair_simplex]{repair_simplex()}}. Use \code{\link[=fixed_gls_covariance]{fixed_gls_covariance()}} for the
global-diagonal competitor
\eqn{\operatorname{diag}\{\sum_j \bar p_j^2\boldsymbol{\Sigma}_j\}}.
This is the mean-only GLS gold standard for covariance-structure
benchmarks, not the DeCovarT convolution
\eqn{\boldsymbol{\Sigma}(\boldsymbol{p})=\sum_j p_j^2\boldsymbol{\Sigma}_j}.
}
\examples{
mu <- cbind(ct1 = c(20, 40), ct2 = c(40, 20))
y <- drop(mu \%*\% c(0.4, 0.6))
Sigma <- array(c(diag(2), 2 * diag(2)), dim = c(2, 2, 2))
deconvolute_ratios_gls(y, mu, fixed_gls_covariance(Sigma))
}
\seealso{
\code{\link[=fixed_gls_covariance]{fixed_gls_covariance()}}, \code{\link[=deconvolute_ratios_deconrnaseq]{deconvolute_ratios_deconrnaseq()}}
}
