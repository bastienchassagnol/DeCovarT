% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_03_run_simulation_benchmark.R
\name{run_simulation_benchmark}
\alias{run_simulation_benchmark}
\title{Simulate bulk mixtures and benchmark deconvolution algorithms}
\usage{
run_simulation_benchmark(
  scenario_config,
  deconvolution_functions,
  n = 200,
  standardise = FALSE,
  scaled = FALSE,
  cores = 1L,
  coverage_interval = "wilson"
)
}
\arguments{
\item{scenario_config}{Tibble or list of scenario rows. Each row must
contain a \code{true_theta} list column (or list element) with at least
\code{mu} and \code{sigma}. Optional per-row \code{n} overrides the default \code{n}.}

\item{deconvolution_functions}{Named list passed to
\code{\link[=deconvolute_ratios]{deconvolute_ratios()}}; each element has \code{FUN} and optional
\code{additional_parameters} for \code{do.call()}.}

\item{n}{Default number of bulk replicates \eqn{N} when \code{scenario_config}
has no \code{n} column.}

\item{standardise, scaled}{Passed to \code{\link[=deconvolute_ratios]{deconvolute_ratios()}}.}

\item{cores}{Workers for the per-sample loop inside
\code{\link[=deconvolute_ratios]{deconvolute_ratios()}}. Defaults to \code{1L}.}

\item{coverage_interval}{Coverage interval for the Monte Carlo
coverage \emph{rate}; see \code{\link[=coverage_mc_interval]{coverage_mc_interval()}}.}
}
\value{
A list with:
\itemize{
\item \code{regression}: \code{global} (per-sample composition scores) and
\code{cell_type} (across-sample Pearson / F1 / spillover);
\item \code{monte_carlo}: ADEMP summaries per cell type;
\item \code{optimisation}: per-sample elapsed time, memory, KKT residual, and
\eqn{\hat{\boldsymbol{p}}};
\item \code{config}: tibble of scenario metadata (one row per scenario);
\item \code{theta_true}: list of convolution parameters
(\eqn{\boldsymbol{p}}, \eqn{\boldsymbol{\mu}}, \eqn{\boldsymbol{\Sigma}_j})
actually used to draw the bulk;
\item \code{descriptors}: kept scenario statistics (composition, mean, SPD,
network, tangent Fisher, MixSim BarOmega, pairwise Hellinger);
\item \code{supplementary}: Jeffreys overlap, recorded separately;
\item \code{call}: the matched call (\code{\link[=match.call]{match.call()}}).
There is no composite global score: each metric answers a different
question.
}
}
\description{
Wrapper around \code{\link[=simulate_bulk_mixture]{simulate_bulk_mixture()}}, \code{\link[=deconvolute_ratios]{deconvolute_ratios()}}, and
\code{\link[=compute_benchmark_metrics]{compute_benchmark_metrics()}} (called inside \code{deconvolute_ratios()}).
Each row of \code{scenario_config} describes one generative model
(\eqn{\boldsymbol{\mu}}, \eqn{(\boldsymbol{\Sigma}_j)_j},
\eqn{\boldsymbol{p}}) stored in a list column \code{true_theta}. Scenario
rows are always evaluated \strong{sequentially} to avoid nested
parallelism; sample-level workers live only in
\code{\link[=deconvolute_ratios]{deconvolute_ratios()}}. Scenario builders (factorial grids, overlap
summaries, etc.) should live in analysis scripts; see
\code{scripts/configure_bivariate_toy_scenarios.R} and the manuscript
scenario vignettes.
}
\examples{
set.seed(1)
genes <- paste0("g", 1:2)
cts <- paste0("ct", 1:2)
mu <- matrix(c(20, 22, 22, 20), nrow = 2, dimnames = list(genes, cts))
Sigma <- array(
  c(1, 0, 0, 1, 1, 0, 0, 1),
  dim = c(2, 2, 2),
  dimnames = list(genes, genes, cts)
)
scenario_config <- tibble::tibble(
  ID = "B1_Ho",
  true_theta = list(list(
    p = c(0.5, 0.5),
    mu = mu,
    sigma = Sigma
  ))
)
out <- run_simulation_benchmark(
  scenario_config = scenario_config,
  deconvolution_functions = list(
    "nnls" = list(FUN = deconvolute_ratios_nnls)
  ),
  n = 2,
  cores = 1
)
nrow(out$optimisation)
}
\seealso{
\code{\link[=simulate_bulk_mixture]{simulate_bulk_mixture()}}, \code{\link[=deconvolute_ratios]{deconvolute_ratios()}},
\code{\link[=compute_benchmark_metrics]{compute_benchmark_metrics()}}, \code{\link[=describe_simulation_scenario]{describe_simulation_scenario()}},
\code{\link[=coverage_mc_interval]{coverage_mc_interval()}}, \code{\link[=plot_mc_raincloud]{plot_mc_raincloud()}}, \code{\link[=plot_mc_forest]{plot_mc_forest()}}
}
