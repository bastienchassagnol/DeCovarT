% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_01_run_benchmark.R
\name{deconvolute_ratios}
\alias{deconvolute_ratios}
\title{Parallel deconvolution of a bulk expression matrix}
\usage{
deconvolute_ratios(
  signature_matrix,
  bulk_expression,
  true_ratios = NULL,
  Sigma = NULL,
  deconvolution_functions = NULL,
  standardise = FALSE,
  scaled = FALSE,
  cores = getOption("mc.cores", 1L)
)
}
\arguments{
\item{signature_matrix}{Mean signature
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (rownames = genes,
colnames = cell types).}

\item{bulk_expression}{Bulk matrix
\eqn{\boldsymbol{Y}\in\mathcal{M}_{G\times N}} (rownames = genes,
colnames = samples).}

\item{true_ratios}{Optional ground-truth proportions: a length-\eqn{J}
numeric vector (recycled over samples) or a numeric matrix
\eqn{J\times N} (or \eqn{N\times J}).}

\item{Sigma}{Optional array
\eqn{(\boldsymbol{\Sigma}_j)_{j}\in\mathcal{M}_{G\times G\times J}}
of cell-type covariances (numeric; off-diagonals may be negative).}

\item{deconvolution_functions}{Named list; each element has \code{FUN} and
optional \code{additional_parameters}.}

\item{standardise}{If \code{TRUE}, apply a \strong{gene-wise} affine z-score
computed once from \eqn{\boldsymbol{\mu}} to bulk, means and covariances
(see Details). Cell-type-wise or sample-wise transforms are not
supported.}

\item{scaled}{Deprecated. \code{TRUE} (log2 mixing) always errors.}

\item{cores}{Number of \code{furrr} workers for the \strong{sample} loop.
Defaults to \code{getOption("mc.cores", 1L)}. Use \code{cores = 1} to stay
sequential (CRAN examples and nested callers).}
}
\value{
A named list from \code{\link[=compute_benchmark_metrics]{compute_benchmark_metrics()}} with
\code{regression} (global and cell-type subtables), \code{monte_carlo}, and
\code{optimisation} (per-sample elapsed time, memory, KKT residual, and
\eqn{\hat{\boldsymbol{p}}}). First-generation solvers still call
\code{\link[=repair_simplex]{repair_simplex()}}; the three native DeCovarT maps (ALR or
\eqn{p/\sum p}) already lie on the simplex.
}
\description{
For each column \eqn{\boldsymbol{y}_{\cdot i}} of the bulk matrix
\eqn{\boldsymbol{Y}\in\mathcal{M}_{G\times N}}, estimates
\eqn{\hat{\boldsymbol{p}}_{\cdot i}} with every supplied algorithm.
Samples are iterated with \code{furrr} when \code{cores > 1}; algorithms are
sequential, so workers are never nested. When covariance information
is provided, DeCovarT methods maximise
\eqn{\ell_{\boldsymbol{y}\,|\,\boldsymbol{\zeta}}(\boldsymbol{p})} under
\eqn{\boldsymbol{y}\,|\,(\boldsymbol{\zeta},\boldsymbol{p})\sim
\mathcal{N}_{G}(
  \boldsymbol{\mu}\boldsymbol{p},
  \boldsymbol{\Sigma}(\boldsymbol{p})
)}.
}
\examples{
set.seed(1)
genes <- paste0("g", 1:2)
cts <- paste0("ct", 1:2)
mu <- matrix(c(20, 22, 22, 20), nrow = 2, dimnames = list(genes, cts))
Sigma <- array(
  c(1, 0, 0, 1, 1, 0, 0, 1),
  dim = c(2, 2, 2),
  dimnames = list(genes, genes, cts)
)
Y <- simulate_bulk_mixture(mu, Sigma, p = c(0.5, 0.5), n = 2)$Y
deconvolute_ratios(
  signature_matrix = mu,
  bulk_expression = Y,
  true_ratios = c(0.5, 0.5),
  Sigma = Sigma,
  deconvolution_functions = list(
    "nnls" = list(FUN = deconvolute_ratios_nnls)
  ),
  cores = 1
)
}
\references{
\insertRef{erkkilaProbabilisticAnalysisGene2010}{DeCovarT}
\insertRef{ahnDeMixDeconvolutionMixed2013}{DeCovarT}
\insertRef{wangTranscriptomeDeconvolutionHeterogeneous2018}{DeCovarT}
\insertRef{anghelISOpureRImplementationComputational2015}{DeCovarT}
\insertRef{ogundijoSequentialMonteCarlo2017}{DeCovarT}
\insertRef{hafemeisterNormalizationVarianceStabilization2019}{DeCovarT}
\insertRef{chionBayesianFrameworkMultivariate2023}{DeCovarT}
\insertRef{newmanRobustEnumerationCell2015}{DeCovarT}
}
\seealso{
\code{\link[=deconvolute_ratios_Marquardt_Levenberg]{deconvolute_ratios_Marquardt_Levenberg()}}
}
