% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_02_covariance_backends.R
\name{new_decovart_covariance}
\alias{new_decovart_covariance}
\alias{print.decovart_covariance}
\title{Structure-aware covariance backend for \eqn{\boldsymbol{\Sigma}(\boldsymbol{p})}}
\usage{
new_decovart_covariance(
  Sigma = NULL,
  structure = c("dense", "block", "band", "sparse", "diag_lowrank"),
  blocks = NULL,
  bandwidth = NULL,
  diagonal = NULL,
  loadings = NULL,
  core = NULL,
  tol = 1e-08
)

\method{print}{decovart_covariance}(x, ...)
}
\arguments{
\item{Sigma}{Numeric array of cell-type covariances in
\eqn{\mathcal{M}_{G\times G\times J}}. For \code{"diag_lowrank"} it may be
omitted and is then reconstructed from \code{diagonal}, \code{loadings}, \code{core}.}

\item{structure}{One of \code{"dense"}, \code{"block"}, \code{"band"}, \code{"sparse"},
\code{"diag_lowrank"}. Matching is case-insensitive.}

\item{blocks}{For \code{"block"}: an integer / factor vector of length
\eqn{G} giving block membership, or a list of index vectors.}

\item{bandwidth}{For \code{"band"}: non-negative integer bandwidth \eqn{b}.}

\item{diagonal}{For \code{"diag_lowrank"}: numeric \eqn{G\times J} matrix whose
column \eqn{j} is the diagonal of \eqn{\mathbf{D}_j}.}

\item{loadings}{For \code{"diag_lowrank"}: shared loadings
\eqn{\mathbf{U}\in\mathcal{M}_{G\times r}}.}

\item{core}{For \code{"diag_lowrank"}: array \eqn{\mathcal{M}_{r\times r\times J}}
of cores \eqn{\mathbf{C}_j}.}

\item{tol}{Relative tolerance for validating that off-structure entries
vanish (block / band).}

\item{x}{A \code{decovart_covariance} object (print method).}

\item{...}{Ignored.}
}
\value{
An object of class \code{"decovart_covariance"}.
}
\description{
Wraps the cell-type covariance array together with a declared
\code{structure}, so that the bulk covariance
\eqn{\boldsymbol{\Sigma}(\boldsymbol{p})=\sum_j p_j^2\boldsymbol{\Sigma}_j}
can be factorised with the cheapest exact method. The object exposes
operators via \code{\link[=sigma_logdet]{sigma_logdet()}}, \code{\link[=sigma_solve]{sigma_solve()}}, \code{\link[=sigma_quadform]{sigma_quadform()}} and
\code{\link[=sigma_trace_precision_times]{sigma_trace_precision_times()}}; the dense array is always accepted and
wrapped as \code{"dense"}.
}
\details{
Supported structures:
\itemize{
\item \code{"dense"}: universal fallback; one Cholesky of the assembled matrix.
\item \code{"block"}: exactly block-diagonal covariance (disconnected gene
modules); one Cholesky per block, so the factorisation cost drops by
roughly the squared number of equal blocks.
\item \code{"band"}: covariance with bandwidth \eqn{b}
(\eqn{\Sigma_{jk}=0} for \eqn{|j-k|>b}); a banded / sparse Cholesky.
\item \code{"sparse"}: sparse covariance with a fixed nonzero pattern across
\eqn{\boldsymbol{p}}; the fill-reducing ordering (symbolic
factorisation) is computed once and reused for every numeric refactor.
\item \code{"diag_lowrank"}: \eqn{\boldsymbol{\Sigma}_j=\mathbf{D}_j+
  \mathbf{U}\mathbf{C}_j\mathbf{U}^{\mathsf{T}}} with shared loadings
\eqn{\mathbf{U}}; solves use the Woodbury identity and the
log-determinant uses the matrix-determinant lemma, transferring the
\eqn{O(G^3)} cost to the low rank \eqn{r\ll G}.
}

Band and sparse factorisations use \pkg{Matrix} (CHOLMOD;
\insertCite{chenAlgorithm887CHOLMOD2008}{DeCovarT}).
Dense Cholesky complexity follows
\insertCite{golubMatrixComputations2013}{DeCovarT};
the low-rank path uses the Woodbury identity
\insertCite{hagerUpdatingInverseMatrix1989}{DeCovarT}.
}
\examples{
Sigma <- array(c(diag(2), diag(2)), dim = c(2, 2, 2))
cov_dense <- new_decovart_covariance(Sigma, "dense")
sigma_logdet(cov_dense, c(0.6, 0.4))
}
\references{
\insertRef{chenAlgorithm887CHOLMOD2008}{DeCovarT}

\insertRef{golubMatrixComputations2013}{DeCovarT}

\insertRef{hagerUpdatingInverseMatrix1989}{DeCovarT}
}
\seealso{
\code{\link[=sigma_logdet]{sigma_logdet()}}, \code{\link[=covariance_structure_from_graph_model]{covariance_structure_from_graph_model()}}
}
\keyword{internal}
