% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_02_benchmark_visualisation.R
\name{plot_algorithm_similarity}
\alias{plot_algorithm_similarity}
\title{Tile heatmap of algorithm-similarity correlations}
\usage{
plot_algorithm_similarity(
  benchmark,
  facet_rows = NULL,
  facet_cols = NULL,
  dendrogram = FALSE
)
}
\arguments{
\item{benchmark}{List from \code{\link[=run_simulation_benchmark]{run_simulation_benchmark()}}.}

\item{facet_rows, facet_cols}{Optional scenario columns. When supplied,
correlations are computed inside each facet cell.}

\item{dendrogram}{If \code{TRUE}, attach a \code{ggdendro} ggplot as attribute
\code{"dendrogram"} (ignored when scenario facets are used).}
}
\value{
A \code{ggplot} object.
}
\description{
\code{ggplot2::geom_tile()} display of \code{\link[=algorithm_similarity]{algorithm_similarity()}}, with
rows and columns ordered by average-linkage clustering of
\(1-r\). Optional dendrogram via \code{ggdendro} (Suggests). This is the
default for a small correlation matrix; \code{\link[=plot_correlation_Heatmap]{plot_correlation_Heatmap()}}
is reserved for linked multi-omics grids.
}
\examples{
\dontshow{if (.Platform$OS.type != "windows") withAutoprint(\{ # examplesIf}
set.seed(1)
genes <- paste0("g", 1:2)
cts <- paste0("ct", 1:2)
mu <- matrix(c(20, 22, 22, 20), nrow = 2, dimnames = list(genes, cts))
Sigma <- array(
  c(1, 0, 0, 1, 1, 0, 0, 1),
  dim = c(2, 2, 2),
  dimnames = list(genes, genes, cts)
)
out <- run_simulation_benchmark(
  tibble::tibble(
    true_theta = list(list(p = c(0.5, 0.5), mu = mu, sigma = Sigma))
  ),
  deconvolution_functions = list(
    "nnls" = list(FUN = deconvolute_ratios_nnls),
    "rlm" = list(FUN = deconvolute_ratios_rlm)
  ),
  n = 4,
  cores = 1
)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_algorithm_similarity(out)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=algorithm_similarity]{algorithm_similarity()}}, \code{\link[=plot_mc_metric_dots]{plot_mc_metric_dots()}}
}
