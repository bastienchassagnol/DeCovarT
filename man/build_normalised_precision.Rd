% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_02_generate_synthetic_networks.R
\name{build_normalised_precision}
\alias{build_normalised_precision}
\title{Complete a weighted adjacency to an SPD precision}
\usage{
build_normalised_precision(weighted_adjacency, precision_shift)
}
\arguments{
\item{weighted_adjacency}{Symmetric numeric matrix (zero diagonal).}

\item{precision_shift}{Positive diagonal cushion \eqn{u}.}
}
\value{
Symmetric positive-definite matrix.
}
\description{
Applies a uniform spectral shift that preserves off-diagonal signs and
support:
\deqn{
  \boldsymbol{\Omega}
  =
  \boldsymbol{W}
  +
  \bigl(\lvert\min(0,\lambda_{\min}(\boldsymbol{W}))\rvert + u\bigr)
  \mathbf{I}.
}
The result is checked with \code{\link[base:chol]{base::chol()}}; if a factor still fails
(ill-conditioned graphs), the same loading is increased until
\eqn{\boldsymbol{\Omega}\succ 0} strictly.
}
\examples{
A <- generate_random_network_skeleton(6L, "hub")
W <- assign_iid_signed_weights(A)
build_normalised_precision(W, precision_shift = 0.2)
}
\keyword{internal}
