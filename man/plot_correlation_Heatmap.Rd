% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_02_benchmark_visualisation.R
\name{plot_correlation_Heatmap}
\alias{plot_correlation_Heatmap}
\title{Plot deconvolution metric heatmaps}
\usage{
plot_correlation_Heatmap(
  distribution_metrics,
  score_variable = "model_mse",
  n_break = 20,
  uni_scale = TRUE,
  file = NULL
)
}
\arguments{
\item{distribution_metrics}{Tibble of metric scores from a benchmark.}

\item{score_variable}{Column name of the metric to display
(\code{"model_mse"}, \code{"model_rmse"}, \code{"model_coef_determination"},
\code{"model_coef_determination_adjusted"}, \code{"model_mae"}, \code{"model_cor"},
\code{"model_ccc"}). Matching is case-insensitive.}

\item{n_break}{Number of colour breaks.}

\item{uni_scale}{If \code{FALSE}, each panel uses its own colour scale.}

\item{file}{Optional PDF path. When supplied, heatmaps are drawn with
\code{\link[grDevices:pdf]{grDevices::pdf()}}; a missing \code{.pdf} suffix is added (G4.0).}
}
\value{
A named list of \code{ComplexHeatmap} heatmap objects (one per algorithm).
}
\description{
For each algorithm, visualises a selected score over the design grid of
simulated \eqn{\boldsymbol{p}} (and related scenario factors).
}
\examples{
metrics <- tibble::tibble(
  correlation_celltype1 = c(0, 0, 0.5, 0.5),
  correlation_celltype2 = c(0, 0.5, 0, 0.5),
  algorithm = "nnls",
  model_mse = c(0.01, 0.02, 0.015, 0.03)
)
ht <- plot_correlation_Heatmap(metrics, score_variable = "model_mse")
names(ht)
}
