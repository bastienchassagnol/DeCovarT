% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_05_decovart_fit.R
\name{vcov_ilr_delta}
\alias{vcov_ilr_delta}
\title{Cramer--Rao / ILR delta-method covariance of \eqn{\hat{\boldsymbol{p}}}}
\usage{
vcov_ilr_delta(p, mean_signature_matrix, Sigma)
}
\arguments{
\item{p}{Numeric proportions on the open simplex.}

\item{mean_signature_matrix}{Mean signature \eqn{\boldsymbol{\mu}}
(\eqn{G\times J}).}

\item{Sigma}{Cell-type covariances \eqn{G\times G\times J}.}
}
\value{
Symmetric \eqn{J\times J} asymptotic covariance of
\eqn{\hat{\boldsymbol{p}}}, or a matrix of \code{NA} if the bound is
undefined / singular.
}
\description{
Maps the expected Fisher information of unconstrained proportions
through the isometric log-ratio (ILR) chart and back to the simplex
via the delta method.

Let \eqn{\boldsymbol{p}=\operatorname{softmax}(\mathbf{V}\boldsymbol{z})}
with Jacobian
\eqn{\mathbf{J}_{\boldsymbol{\psi}}=\mathbf{S}(\boldsymbol{p})
\mathbf{V}}
(\code{\link[=jacobian_isometric_logistic]{jacobian_isometric_logistic()}}). Fisher information transforms as
\deqn{
  I_{\boldsymbol{z}}
  =
  \mathbf{J}_{\boldsymbol{\psi}}^{\mathsf{T}}
  I(\boldsymbol{p})
  \mathbf{J}_{\boldsymbol{\psi}}
  =
  \mathbf{V}^{\mathsf{T}}
  \mathbf{S}(\boldsymbol{p})
  I(\boldsymbol{p})
  \mathbf{S}(\boldsymbol{p})
  \mathbf{V}.
}
The first-order delta method then yields
\deqn{
  \mathrm{Var}(\hat{\boldsymbol{p}})
  \approx
  \mathbf{J}_{\boldsymbol{\psi}}
  I_{\boldsymbol{z}}^{-1}
  \mathbf{J}_{\boldsymbol{\psi}}^{\mathsf{T}}.
}
This simplex covariance is invariant to orthogonal rotations of
\eqn{\mathbf{V}}. The construction is undefined on the simplex
boundary (the log-ratio chart blows up); the function then returns
\code{NA} with a warning. \code{\link[=vcov_alr_delta]{vcov_alr_delta()}} is the ALR-chart analogue
used only for reference-invariance checks.
}
\seealso{
\code{\link[=expected_fisher_unconstrained]{expected_fisher_unconstrained()}}, \code{\link[=vcov.decovart_fit]{vcov.decovart_fit()}},
\code{\link[=confint.decovart_fit]{confint.decovart_fit()}}, \code{\link[=jacobian_isometric_logistic]{jacobian_isometric_logistic()}},
\code{\link[=vcov_alr_delta]{vcov_alr_delta()}}
}
\keyword{internal}
