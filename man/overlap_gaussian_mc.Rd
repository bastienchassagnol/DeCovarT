% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_metrics_monte_carlo_overlap.R
\name{overlap_gaussian_mc}
\alias{overlap_gaussian_mc}
\title{MixSim-style pairwise overlap via stratified Sobol Monte Carlo}
\usage{
overlap_gaussian_mc(true_theta, n_mc = 10000L, seed = NULL, J = NULL)
}
\arguments{
\item{true_theta}{List with \code{p}, \code{mu}, \code{sigma} as in
\code{\link[=check_true_theta]{check_true_theta()}}.}

\item{n_mc}{Draws \strong{per component} (default \code{10000}).}

\item{seed}{Optional RNG seed (passed to Sobol randomisation and to
the uniform fallback).}

\item{J}{Optional number of components.}
}
\value{
A list with \code{BarOmega}, \code{MaxOmega}, and \code{OmegaMap}.
}
\description{
Estimates the MixSim Omega map
\eqn{\Omega_{j\ell}=\Pr_{X\sim f_j}(\pi_\ell f_\ell(X)>\pi_j f_j(X))}
by drawing \code{n_mc} quasi-Monte Carlo points \strong{from each component}
(inverse-transform sampling of a Sobol sequence through the
Cholesky factor) and comparing \strong{log-densities}. The average
pairwise overlap \code{BarOmega} is the unweighted mean of
\eqn{\Omega_{j\ell}+\Omega_{\ell j}} over \eqn{j<\ell}, matching
\code{\link[MixSim:overlap]{MixSim::overlap()}} / \href{https://rosa.unipr.it/FSDA/MixSim.html}{FSDA MixSim}.
}
\details{
For two equal-weight components this pairwise overlap equals the
histogram similarity \eqn{\int\min(f_j,f_\ell)} and therefore
\eqn{1-\mathrm{TV}(f_j,f_\ell)}
\insertCite{nielsenGuaranteedDeterministicBounds2018}{DeCovarT}.
}
\examples{
set.seed(1)
theta <- list(
  p = c(0.5, 0.5),
  mu = cbind(c(0, 0), c(3, 0)),
  sigma = array(c(diag(2), diag(2)), dim = c(2, 2, 2))
)
overlap_gaussian_mc(theta, n_mc = 2000L)$BarOmega
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=compute_average_overlap]{compute_average_overlap()}}, \code{\link[=compute_average_riemannian]{compute_average_riemannian()}}
}
