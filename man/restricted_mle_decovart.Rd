% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_06_decovart_inference.R
\name{restricted_mle_decovart}
\alias{restricted_mle_decovart}
\title{Restricted maximum likelihood with fixed cellular ratios}
\usage{
restricted_mle_decovart(
  y,
  mean_signature_matrix,
  Sigma,
  fixed,
  epsilon = 10^-8,
  itmax = 500L
)
}
\arguments{
\item{y}{Numeric vector (or one-column matrix)
\eqn{\boldsymbol{y}\in\mathbb{R}^{G}}.}

\item{mean_signature_matrix}{Numeric matrix
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (plug-in means).}

\item{Sigma}{Array of cell-type covariances in
\eqn{\mathcal{M}_{G\times G\times J}}.}

\item{fixed}{Named or integer-indexed numeric vector of constrained
ratios, e.g. \code{c(celltype_1 = 0)}. Names are matched against
\code{colnames(mean_signature_matrix)}.}

\item{epsilon, itmax}{Relative convergence tolerance and iteration
budget passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A list with \code{coefficients} (the restricted \eqn{J} vector),
\code{loglik}, \code{fixed}, and \code{convergence}.
}
\description{
Maximises the DeCovarT log-likelihood over the simplex while holding a
subset of coordinates at prescribed values. Writing \eqn{A} for the
constrained index set and \eqn{s=\sum_{j\in A}c_j}, the free block is
reparametrised as
\deqn{
  \boldsymbol{p}_{A^{c}}
  =
  (1-s)\,\boldsymbol{\psi}(\boldsymbol{z}),
  \qquad
  \boldsymbol{z}\in\mathbb{R}^{|A^{c}|-1},
}
with \eqn{\boldsymbol{\psi}} the isometric logistic map
(\code{\link[=isometric_logistic]{isometric_logistic()}}) on a Helmert basis of the free face.
The constrained coordinates are \emph{substituted}
rather than pushed through a logarithm, so a null such as
\eqn{p_j=0} is representable exactly: this is what makes boundary
likelihood-ratio tests computable (see \code{\link[=lrt_decovart]{lrt_decovart()}}).
}
\details{
Zero mass on \eqn{A} keeps
\eqn{\boldsymbol{\Sigma}(\boldsymbol{p})=\sum_j p_j^{2}
\boldsymbol{\Sigma}_j} positive definite as long as at least one
coordinate remains strictly positive. Optimisation uses
\code{\link[stats:optim]{stats::optim()}} (\code{"BFGS"}) with the analytic score obtained by
chaining \code{\link[=gradient_loglik_unconstrained]{gradient_loglik_unconstrained()}} through
\eqn{(1-s)\mathbf{J}_{\boldsymbol{\psi}}} on the free-face ILR chart.
}
\examples{
mu <- matrix(c(20, 22, 18, 22, 20, 24), nrow = 2)
colnames(mu) <- paste0("ct", 1:3)
Sigma <- array(c(diag(2), diag(2), diag(2)), dim = c(2, 2, 3))
y <- drop(mu \%*\% c(0.5, 0.5, 0))
restricted_mle_decovart(y, mu, Sigma, fixed = c(ct3 = 0))$coefficients
}
\seealso{
\code{\link[=lrt_decovart]{lrt_decovart()}}, \code{\link[=profile_loglik_decovart]{profile_loglik_decovart()}},
\code{\link[=loglik_multivariate]{loglik_multivariate()}}

Other decovart_inference: 
\code{\link{bootstrap_decovart}()},
\code{\link{boundary_diagnostics}()},
\code{\link{chi_bar_square_pvalue}()},
\code{\link{confint_profile_decovart}()},
\code{\link{equivariance_check_decovart}()},
\code{\link{lrt_decovart}()},
\code{\link{multistart_decovart}()},
\code{\link{profile_loglik_decovart}()},
\code{\link{reference_bootstrap_decovart}()}
}
\concept{decovart_inference}
\keyword{internal}
