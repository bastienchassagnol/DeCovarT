% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_05_hybrid_latent_visualisation.R
\name{save_bivariate_memory_book}
\alias{save_bivariate_memory_book}
\title{Solver peak memory for the bivariate toy (12 pages)}
\usage{
save_bivariate_memory_book(artefacts, file, data_rds = NULL)
}
\arguments{
\item{artefacts}{Split or assembled fig03 artefacts from
\code{\link[=read_simulation_artefacts]{read_simulation_artefacts()}}.}

\item{file}{Output PDF path.}

\item{data_rds}{Optional directory for ggplot \code{data} RDS files.}
}
\value{
\code{file}, invisibly.
}
\description{
Same layout as \code{\link[=save_bivariate_runtime_book]{save_bivariate_runtime_book()}}. Memory is plotted in
mebibytes (\code{memory_bytes / 2^20}) on a log10 y-axis.
}
\examples{
if (requireNamespace("ggdist", quietly = TRUE)) {
  opt <- tibble::tibble(
    ID = rep(c("A", "B", "C", "D"), each = 8),
    sample_id = paste0("s", 1:32),
    algorithm = rep(c("lsei", "LBFGS"), 16),
    memory_bytes = runif(32, 3.8e8, 4.2e8)
  )
  cfg <- tibble::tibble(
    ID = c("A", "B", "C", "D"),
    centroids = "small_CLD",
    variance = "homoscedastic",
    proportions = "balanced",
    correlation_celltype1 = c(0, -0.8, 0.8, -0.8),
    correlation_celltype2 = c(0, -0.8, 0.8, 0.8)
  )
  artefacts <- list(config = cfg, optimisation = opt)
  tf <- withr::local_tempfile(fileext = ".pdf")
  save_bivariate_memory_book(artefacts, tf)
}
}
\seealso{
\code{\link[=save_bivariate_runtime_book]{save_bivariate_runtime_book()}}
}
