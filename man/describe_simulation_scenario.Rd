% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_03_scenario_descriptors.R
\name{describe_simulation_scenario}
\alias{describe_simulation_scenario}
\title{Describe a Gaussian-convolution simulation scenario}
\usage{
describe_simulation_scenario(true_theta, adjacency = NULL, active_tol = 1e-08)
}
\arguments{
\item{true_theta}{List with \code{p}, \code{mu}, and \code{sigma} (and optionally
\code{Theta} precision). Parsed by \code{\link[=check_true_theta]{check_true_theta()}} /
\code{.parse_true_theta()}.}

\item{adjacency}{Optional \eqn{G\times G\times J} adjacency array, or
a length-\eqn{J} list of adjacencies, used for network density when
supplied.}

\item{active_tol}{Threshold for counting an active simplex component.}
}
\value{
A list with:
\itemize{
\item \code{theta_true}: the convolution parameters \code{p}, \code{mu}, \code{sigma};
\item \code{descriptors}: one-row tibble of kept scenario statistics in six
families (composition, mean geometry, SPD of
\eqn{\boldsymbol{\Sigma}(\boldsymbol{p})}, tangent Fisher, network,
component overlap). SPD columns include both
\eqn{\kappa\{\boldsymbol{\Sigma}(\boldsymbol{p})\}}
(\code{kappa_sigma_p}) and the reciprocal
\eqn{\lambda_{\min}/\lambda_{\max}} (\code{kappa_sigma_reciprocal});
\item \code{supplementary}: one-row tibble of Jeffreys / symmetrised KL,
recorded but not treated as a primary score;
\item \code{call}: the matched call (\code{\link[=match.call]{match.call()}}).
}
}
\description{
Summarises the generating law
\eqn{\boldsymbol{y}\mid(\boldsymbol{\zeta},\boldsymbol{p})\sim
\mathcal{N}_{G}(\boldsymbol{\mu}\boldsymbol{p},
\sum_j p_j^2\boldsymbol{\Sigma}_j)} at five layers: composition,
mean geometry, covariance / SPD diagnostics, network sparsity, and
tangent Fisher information (mean versus covariance split). MixSim
BarOmega and average pairwise Hellinger of the component Gaussians
are kept in \code{descriptors}. Jeffreys / symmetrised KL is returned in
\code{supplementary}.
}
\examples{
theta <- list(
  p = c(0.5, 0.5),
  mu = cbind(c(10, 0), c(0, 10)),
  sigma = array(c(diag(2), diag(2)), dim = c(2, 2, 2))
)
out <- describe_simulation_scenario(theta)
out$descriptors$h_star
out$call
}
\seealso{
\code{\link[=run_simulation_benchmark]{run_simulation_benchmark()}}, \code{\link[=expected_fisher_unconstrained]{expected_fisher_unconstrained()}},
\code{\link[=compute_shannon_entropy]{compute_shannon_entropy()}}, \code{\link[=composition_from_entropy]{composition_from_entropy()}},
\code{\link[=helmert_basis]{helmert_basis()}}
}
