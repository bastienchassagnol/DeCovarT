% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_06_decovart_inference.R
\name{lrt_decovart}
\alias{lrt_decovart}
\title{Likelihood-ratio test for cellular ratios}
\usage{
lrt_decovart(
  y = NULL,
  mean_signature_matrix,
  Sigma,
  null_value,
  bulk_expression = NULL,
  n_boundary = NULL,
  boundary_tol = 1e-06,
  epsilon = 10^-8,
  itmax = 500L
)
}
\arguments{
\item{y}{Numeric vector (or one-column matrix)
\eqn{\boldsymbol{y}\in\mathbb{R}^{G}}.}

\item{mean_signature_matrix}{Numeric matrix
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (plug-in means).}

\item{Sigma}{Array of cell-type covariances in
\eqn{\mathcal{M}_{G\times G\times J}}.}

\item{null_value}{Named numeric vector of hypothesised ratios, e.g.
\code{c(celltype_2 = 0)}.}

\item{bulk_expression}{Numeric matrix \eqn{\boldsymbol{Y}\in
\mathcal{M}_{G\times N}} of replicate bulk profiles sharing one
composition, used instead of \code{y} for the pooled (population) test.}

\item{n_boundary}{Optional integer override for the number of active
boundary constraints; \code{NULL} infers it from \code{null_value}.}

\item{boundary_tol}{Distance from 0 or 1 below which a null value
counts as being on the boundary.}

\item{epsilon, itmax}{Relative convergence tolerance and iteration
budget passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A one-row data frame with columns \code{statistic},
\code{n_boundary}, \code{df_interior}, \code{p_value}, \code{loglik_full},
\code{loglik_null}, and \code{calibration}.
}
\description{
Tests \eqn{H_0:p_j=c_j,\,j\in A} against the unrestricted simplex with
the profile likelihood-ratio statistic
\deqn{
  D
  =
  2\bigl\{
    \ell(\hat{\boldsymbol{p}})
    -\ell(\hat{\boldsymbol{p}}^{(0)})
  \bigr\},
}
where \eqn{\hat{\boldsymbol{p}}^{(0)}} is the restricted MLE of
\code{\link[=restricted_mle_decovart]{restricted_mle_decovart()}}. Unlike the Wald interval of
\code{\link[=confint.decovart_fit]{confint.decovart_fit()}}, \eqn{D} is invariant under the additive
log-ratio reparametrisation, because a smooth bijection leaves
likelihood ratios unchanged.
}
\details{
\strong{Interior null.} If every tested ratio lies strictly inside
\eqn{(0,1)}, \eqn{D} is asymptotically \eqn{\chi^{2}_{|A|}}
\insertCite{wilksLargeSampleDistributionLikelihood1938}{DeCovarT}.

\strong{Boundary null.} A null such as \eqn{p_j=0} lets the parameter move
in one direction only, so the local parameter space is a half-line
rather than a vector space and Wilks' theorem fails. The limit is the
chi-bar-square mixture of \code{\link[=chi_bar_square_pvalue]{chi_bar_square_pvalue()}}: for a single
active constraint,
\eqn{D\rightsquigarrow\tfrac{1}{2}\chi^{2}_{0}+\tfrac{1}{2}\chi^{2}_{1}}
\insertCite{hermanDistributionLikelihoodRatio1954;textual}{DeCovarT},
generalised to several active constraints by
\insertCite{selfAsymptoticPropertiesMaximum1987;textual}{DeCovarT}.
Boundary calibration is selected automatically when any tested value is
within \code{boundary_tol} of 0 or 1; override with \code{n_boundary}.

\strong{Replication.} Both calibrations are \emph{population} statements: they
need replicate bulk samples that genuinely share one composition
\eqn{\boldsymbol{p}}. A single \eqn{G}-vector supplies no replication
(genes are dependent through \eqn{\boldsymbol{\Sigma}(\boldsymbol{p})},
and adding bulk samples with distinct compositions adds parameters as
fast as observations). Use \code{bulk_expression} with several replicate
columns, or calibrate by \code{\link[=bootstrap_decovart]{bootstrap_decovart()}}.
}
\examples{
mu <- matrix(c(20, 22, 18, 22, 20, 24), nrow = 2)
colnames(mu) <- paste0("ct", 1:3)
Sigma <- array(c(diag(2), diag(2), diag(2)), dim = c(2, 2, 3))
y <- drop(mu \%*\% c(0.5, 0.5, 0))
lrt_decovart(y, mu, Sigma, null_value = c(ct3 = 0))
}
\references{
\insertRef{wilksLargeSampleDistributionLikelihood1938}{DeCovarT}

\insertRef{selfAsymptoticPropertiesMaximum1987}{DeCovarT}
}
\seealso{
\code{\link[=chi_bar_square_pvalue]{chi_bar_square_pvalue()}}, \code{\link[=confint_profile_decovart]{confint_profile_decovart()}},
\code{\link[=bootstrap_decovart]{bootstrap_decovart()}}

Other decovart_inference:
\code{\link[=bootstrap_decovart]{bootstrap_decovart()}},
\code{\link[=boundary_diagnostics]{boundary_diagnostics()}},
\code{\link[=chi_bar_square_pvalue]{chi_bar_square_pvalue()}},
\code{\link[=confint_profile_decovart]{confint_profile_decovart()}},
\code{\link[=equivariance_check_decovart]{equivariance_check_decovart()}},
\code{\link[=multistart_decovart]{multistart_decovart()}},
\code{\link[=profile_loglik_decovart]{profile_loglik_decovart()}},
\code{\link[=reference_bootstrap_decovart]{reference_bootstrap_decovart()}},
\code{\link[=restricted_mle_decovart]{restricted_mle_decovart()}}
}
\concept{decovart_inference}
