% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_01_first_generation_deconvolution.R,
%   R/03_03_DeCovarT_estimate_ratios_frequentist.R
\name{deconvolute_ratios_cibersort}
\alias{deconvolute_ratios_cibersort}
\alias{deconvolute_ratios_rlm}
\alias{deconvolute_ratios_nnls}
\alias{deconvolute_ratios_deconrnaseq}
\alias{deconvolute_ratios_Marquardt_Levenberg}
\alias{deconvolute_ratios_simulated_annealing}
\alias{deconvolute_ratios_L_BFGS_B}
\alias{deconvolute_ratios_Newton_Raphson}
\alias{deconvolute_ratios_gradient_descent}
\title{DeCovarT MLE of cellular proportions for one bulk sample}
\usage{
deconvolute_ratios_cibersort(y, mean_signature_matrix)

deconvolute_ratios_rlm(y, mean_signature_matrix)

deconvolute_ratios_nnls(y, mean_signature_matrix)

deconvolute_ratios_deconrnaseq(y, mean_signature_matrix)

deconvolute_ratios_Marquardt_Levenberg(
  y,
  mean_signature_matrix,
  Sigma,
  epsilon = 10^-4,
  itmax = 200,
  return_model = FALSE,
  initial_p = NULL,
  dirichlet_alpha = 1
)

deconvolute_ratios_simulated_annealing(
  y,
  mean_signature_matrix,
  Sigma,
  epsilon = 10^-4,
  itmax = 200,
  initial_p = NULL,
  dirichlet_alpha = 1
)

deconvolute_ratios_L_BFGS_B(
  y,
  mean_signature_matrix,
  Sigma,
  epsilon = 10^-4,
  itmax = 200,
  return_model = FALSE,
  initial_p = NULL,
  dirichlet_alpha = 1
)

deconvolute_ratios_Newton_Raphson(
  y,
  mean_signature_matrix,
  Sigma,
  epsilon = 10^-4,
  itmax = 200,
  return_model = FALSE,
  initial_p = NULL,
  dirichlet_alpha = 1
)

deconvolute_ratios_gradient_descent(
  y,
  mean_signature_matrix,
  Sigma,
  epsilon = 10^-4,
  itmax = 200,
  initial_p = NULL,
  dirichlet_alpha = 1
)
}
\arguments{
\item{y}{Numeric vector (or one-column matrix)
\eqn{\boldsymbol{y}\in\mathbb{R}^{G}}.}

\item{mean_signature_matrix}{Numeric matrix
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (plug-in means).}

\item{Sigma}{Array of cell-type covariances in
\eqn{\mathcal{M}_{G\times G\times J}}.}

\item{epsilon, itmax}{Absolute convergence tolerance and maximum number of
iterations for the optimiser (same roles as \code{reltol} / \code{maxit} in
\code{\link[stats:optim]{stats::optim()}}).}

\item{return_model}{If \code{TRUE}, return a named list with coefficients,
ILR coordinates, log-likelihood and optimiser diagnostics instead of
the proportion vector.}

\item{initial_p}{Optional start: \code{NULL} / \code{"barycentre"} (default
equi-balanced), a length-\eqn{J} numeric vector, \code{"dirichlet"}, or
\code{"qp"} (mean-only simplex QP). See \code{\link[=starting_simplex]{starting_simplex()}}. Starts
on a simplex face are nudged into the interior so the ILR map is
defined (ILR methods) and so L-BFGS-B does not start on a
degenerate \eqn{\boldsymbol{\Sigma}(\boldsymbol{p})}.}

\item{dirichlet_alpha}{Dirichlet concentration when
\code{initial_p = "dirichlet"} (default \code{1}: uniform on the simplex;
\eqn{\alpha>1} centre-biased; \eqn{\alpha<1} face-biased).
Independent draws are independent restarts; use
\code{\link[=multistart_decovart]{multistart_decovart()}} to sequence several.}
}
\value{
Named numeric vector \eqn{\hat{\boldsymbol{p}}} on the simplex
(ILR methods), or that list when \code{return_model = TRUE}.
Benchmark metrics are computed by \code{\link[=deconvolute_ratios]{deconvolute_ratios()}}.
}
\description{
Estimates
\eqn{\hat{\boldsymbol{p}}=\arg\max_{\boldsymbol{p}}
\ell_{\boldsymbol{y}\,|\,\boldsymbol{\zeta}}(\boldsymbol{p})} under the
Gaussian convolution model
\deqn{
  \boldsymbol{y}\,|\,(\boldsymbol{\zeta},\boldsymbol{p})
  \sim\mathcal{N}_{G}\!\bigl(\boldsymbol{\mu}\boldsymbol{p},\,
  \boldsymbol{\Sigma}(\boldsymbol{p})\bigr),
  \qquad
  \boldsymbol{\Sigma}(\boldsymbol{p})=\sum_{j=1}^{J}p_j^{2}\boldsymbol{\Sigma}_j,
}
subject to the simplex constraint
\eqn{\mathbf{1}^{\mathsf{T}}\boldsymbol{p}=1}, \eqn{\boldsymbol{p}\ge\mathbf{0}}.
Optimisation is performed in unconstrained ILR coordinates
\eqn{\boldsymbol{z}\in\mathbb{R}^{J-1}} via
\eqn{\boldsymbol{p}=\operatorname{softmax}(\mathbf{V}\boldsymbol{z})}
(Marquardt–Levenberg default; see other methods below and
\code{vignette("theory-decovart-generative-model", package = "DeCovarT")}).
}
\details{
\strong{Plug-in signature.} Argument \code{mean_signature_matrix} is the mean
\eqn{\boldsymbol{\mu}}, used as a proxy for the unobserved cell-type
profiles \eqn{\boldsymbol{x}_{\cdot j}}. This is the frequentist plug-in;
recovering sample-specific latents is a Bayesian / MAP problem
(\code{.map_gaussian_convolution()}).
}
\section{Functions}{
\itemize{
\item \code{deconvolute_ratios_cibersort()}: Linear baseline
\eqn{\hat{\boldsymbol{y}}=\boldsymbol{\mu}\hat{\boldsymbol{p}}} via nu-SVR
(CIBERSORT-style); no covariance prior is used.

\item \code{deconvolute_ratios_rlm()}: Robust linear model
\eqn{\boldsymbol{y}\approx\boldsymbol{\mu}\boldsymbol{p}}
(\code{\link[MASS:rlm]{MASS::rlm()}}), as in
\insertCite{monacoRNASeqSignaturesNormalized2019;textual}{DeCovarT}.

\item \code{deconvolute_ratios_nnls()}: Non-negative least squares
for \eqn{\boldsymbol{y}\approx\boldsymbol{\mu}\boldsymbol{p}}
(\code{\link[nnls:nnls]{nnls::nnls()}}), then simplex projection.

\item \code{deconvolute_ratios_deconrnaseq()}: Equality- and
inequality-constrained least squares on the simplex
(\code{\link[limSolve:lsei]{limSolve::lsei()}}), in the spirit of \code{deconRNASeq}.

\item \code{deconvolute_ratios_simulated_annealing()}: Simulated annealing on
ILR coordinates \eqn{\boldsymbol{z}} (\code{\link[stats:optim]{stats::optim()}} with
\code{method = "SANN"}).

\item \code{deconvolute_ratios_L_BFGS_B()}: Box-constrained L-BFGS-B
directly in \eqn{\boldsymbol{p}} (\code{\link[stats:optim]{stats::optim()}} \code{method = "L-BFGS-B"}).
The box keeps each coordinate in \eqn{[0,1]}; the returned vector is
closed by \eqn{p/\sum p} (no \code{\link[=repair_simplex]{repair_simplex()}} clipping).

\item \code{deconvolute_ratios_Newton_Raphson()}: Newton–Raphson /
\code{nlminb} on ILR coordinates \eqn{\boldsymbol{z}} using analytic
gradient and Hessian (\code{\link[stats:nlminb]{stats::nlminb()}}).

\item \code{deconvolute_ratios_gradient_descent()}: BFGS quasi-Newton ascent
on ILR coordinates \eqn{\boldsymbol{z}} (\code{\link[stats:optim]{stats::optim()}}
\code{method = "BFGS"}).

}}
\examples{
set.seed(1)
genes <- paste0("g", 1:2)
cts <- paste0("ct", 1:2)
mu <- matrix(c(20, 22, 22, 20), nrow = 2, dimnames = list(genes, cts))
Sigma <- array(
  c(1, 0, 0, 1, 1, 0, 0, 1),
  dim = c(2, 2, 2),
  dimnames = list(genes, genes, cts)
)
y <- drop(mu \%*\% c(0.6, 0.4) + rnorm(2, sd = 0.1))
deconvolute_ratios_Marquardt_Levenberg(y, mu, Sigma, itmax = 50)
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=deconvolute_ratios]{deconvolute_ratios()}}, \code{\link[=isometric_logistic]{isometric_logistic()}},
\code{\link[=starting_simplex]{starting_simplex()}}
}
