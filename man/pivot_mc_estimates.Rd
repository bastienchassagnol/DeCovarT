% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_02_benchmark_visualisation.R
\name{pivot_mc_estimates}
\alias{pivot_mc_estimates}
\title{Pivot Monte Carlo proportion estimates to a long table}
\usage{
pivot_mc_estimates(benchmark)
}
\arguments{
\item{benchmark}{List returned by \code{\link[=run_simulation_benchmark]{run_simulation_benchmark()}}.}
}
\value{
A tibble with \code{algorithm}, \code{cell_type}, \code{estimate}, \code{p_true},
\code{error}, \code{sample_id}, and any scenario metadata columns.
}
\description{
Turns the \code{optimisation} block of \code{\link[=run_simulation_benchmark]{run_simulation_benchmark()}} into
one row per replicate, algorithm, and cell type, with the matching
true proportion from \code{theta_true}. The raincloud intervals drawn from
this table are \strong{empirical Monte Carlo quantiles} of
\eqn{\hat{\boldsymbol{p}}}, not confidence intervals for
\eqn{\boldsymbol{p}} \insertCite{allenRaincloudPlotsMultiplatform2019}{DeCovarT}.
}
\examples{
set.seed(1)
genes <- paste0("g", 1:2)
cts <- paste0("ct", 1:2)
mu <- matrix(c(20, 22, 22, 20), nrow = 2, dimnames = list(genes, cts))
Sigma <- array(
  c(1, 0, 0, 1, 1, 0, 0, 1),
  dim = c(2, 2, 2),
  dimnames = list(genes, genes, cts)
)
out <- run_simulation_benchmark(
  tibble::tibble(
    ID = "B1",
    true_theta = list(list(p = c(0.5, 0.5), mu = mu, sigma = Sigma))
  ),
  deconvolution_functions = list(
    "nnls" = list(FUN = deconvolute_ratios_nnls)
  ),
  n = 2,
  cores = 1
)
nrow(pivot_mc_estimates(out))
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=plot_mc_raincloud]{plot_mc_raincloud()}}, \code{\link[=plot_mc_forest]{plot_mc_forest()}}
}
