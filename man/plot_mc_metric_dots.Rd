% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_02_benchmark_visualisation.R
\name{plot_mc_metric_dots}
\alias{plot_mc_metric_dots}
\title{Faceted dot plot of several ADEMP metrics}
\usage{
plot_mc_metric_dots(
  benchmark,
  facet_rows = NULL,
  facet_cols = NULL,
  metrics = c("rmse", "mae", "coverage", "mean_interval_width"),
  weights = NULL
)
}
\arguments{
\item{benchmark}{List from \code{\link[=run_simulation_benchmark]{run_simulation_benchmark()}}, or a table
from \code{\link[=pivot_mc_estimates]{pivot_mc_estimates()}}.}

\item{facet_rows, facet_cols}{Optional column names for
\code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}} rows and columns.}

\item{metrics}{Character vector of summaries to display.}

\item{weights}{Optional named numeric weights (sum to 1) used only
to add a \code{composite} facet beside the components.}
}
\value{
A \code{ggplot} object.
}
\description{
One panel per metric; colour is a \strong{min-max normalised} score with
\(1\) = best inside that metric (and facet). Do not map a second
primary score to point size. Coverage is scored as
\eqn{|\widehat C-0.95|}, so over-wide intervals that inflate
coverage are not rewarded. There is no default weighted composite.
}
\examples{
\dontshow{if (.Platform$OS.type != "windows") withAutoprint(\{ # examplesIf}
set.seed(1)
genes <- paste0("g", 1:2)
cts <- paste0("ct", 1:2)
mu <- matrix(c(20, 22, 22, 20), nrow = 2, dimnames = list(genes, cts))
Sigma <- array(
  c(1, 0, 0, 1, 1, 0, 0, 1),
  dim = c(2, 2, 2),
  dimnames = list(genes, genes, cts)
)
out <- run_simulation_benchmark(
  tibble::tibble(
    cosine = 0,
    true_theta = list(list(p = c(0.5, 0.5), mu = mu, sigma = Sigma))
  ),
  deconvolution_functions = list(
    "nnls" = list(FUN = deconvolute_ratios_nnls)
  ),
  n = 4,
  cores = 1
)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_mc_metric_dots(out, facet_cols = "cosine")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot_mc_forest]{plot_mc_forest()}}, \code{\link[=plot_algorithm_similarity]{plot_algorithm_similarity()}}
}
