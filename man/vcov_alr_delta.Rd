% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_05_decovart_fit.R
\name{vcov_alr_delta}
\alias{vcov_alr_delta}
\title{Cramer--Rao / ALR delta-method covariance of \eqn{\hat{\boldsymbol{p}}}}
\usage{
vcov_alr_delta(p, mean_signature_matrix, Sigma)
}
\arguments{
\item{p}{Numeric proportions on the open simplex.}

\item{mean_signature_matrix}{Mean signature \eqn{\boldsymbol{\mu}}
(\eqn{G\times J}).}

\item{Sigma}{Cell-type covariances \eqn{G\times G\times J}.}
}
\value{
Symmetric \eqn{J\times J} asymptotic covariance of
\eqn{\hat{\boldsymbol{p}}}, or a matrix of \code{NA} if the bound is
undefined / singular.
}
\description{
Maps the expected Fisher information of unconstrained proportions
through the additive log-ratio (ALR) chart and back to the simplex
via the delta method.

Let \eqn{\boldsymbol{p}=\boldsymbol{\psi}(\boldsymbol{\rho})} with
Jacobian
\eqn{\mathbf{J}_{\boldsymbol{\psi}}
=\partial\boldsymbol{\psi}/\partial\boldsymbol{\rho}^{\top}}
(\code{\link[=jacobian_additive_logistic]{jacobian_additive_logistic()}}). Fisher information transforms as
the covariant quadratic form
\deqn{
  I_{\boldsymbol{\rho}}
  =
  \mathbf{J}_{\boldsymbol{\psi}}^{\top}
  I(\boldsymbol{p})
  \mathbf{J}_{\boldsymbol{\psi}}.
}
Under a regular large-sample regime the MLE in ALR coordinates is
asymptotically normal,
\eqn{\hat{\boldsymbol{\rho}}
\overset{a}{\sim}
\mathcal{N}(\boldsymbol{\rho}_{0}, I_{\boldsymbol{\rho}}^{-1})}
(Cramer--Rao / asymptotic normality of MLEs; law of large numbers
for the score). The first-order delta method then yields the
simplex covariance used by \code{\link[=vcov.decovart_fit]{vcov.decovart_fit()}} and the Wald
standard errors in \code{\link[=confint.decovart_fit]{confint.decovart_fit()}}:
\deqn{
  \mathrm{Var}(\hat{\boldsymbol{p}})
  \approx
  \mathbf{J}_{\boldsymbol{\psi}}
  I_{\boldsymbol{\rho}}^{-1}
  \mathbf{J}_{\boldsymbol{\psi}}^{\top}.
}
Diagonal square roots of this matrix are the asymptotic standard
errors; Wald intervals at level \eqn{1-\alpha} are
\eqn{\hat{p}_j \pm z_{1-\alpha/2}\,\mathrm{SE}_j} with
\eqn{z_{q}=\Phi^{-1}(q)}. The construction is undefined on the
simplex boundary (ALR chart blows up); the function then returns
\code{NA} with a warning. See also
\url{https://en.wikipedia.org/wiki/Delta_method} and
\url{https://en.wikipedia.org/wiki/Fisher_information#Multivariate_normal_distribution}.
}
\seealso{
\code{\link[=expected_fisher_unconstrained]{expected_fisher_unconstrained()}}, \code{\link[=vcov.decovart_fit]{vcov.decovart_fit()}},
\code{\link[=confint.decovart_fit]{confint.decovart_fit()}}, \code{\link[=jacobian_additive_logistic]{jacobian_additive_logistic()}}
}
\keyword{internal}
