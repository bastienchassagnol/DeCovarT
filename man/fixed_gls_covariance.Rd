% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_01_first_generation_deconvolution.R
\name{fixed_gls_covariance}
\alias{fixed_gls_covariance}
\title{Fixed residual covariance for a GLS competitor}
\usage{
fixed_gls_covariance(Sigma, p = NULL, diagonal = TRUE)
}
\arguments{
\item{Sigma}{Array \eqn{G\times G\times J} of cell-type covariances.}

\item{p}{Optional composition of length \eqn{J}; default
\eqn{(1/J,\ldots,1/J)}.}

\item{diagonal}{If \code{TRUE} (default), return
\eqn{\operatorname{diag}\{\boldsymbol{\Sigma}(\boldsymbol{p})\}}.}
}
\value{
A \eqn{G\times G} covariance matrix.
}
\description{
Evaluates the convolution covariance
\eqn{\boldsymbol{\Sigma}(\boldsymbol{p})=\sum_j p_j^2\boldsymbol{\Sigma}_j}
at a \strong{known} composition (default the barycentre
\eqn{p_j=1/J}) and optionally keeps only the diagonal. That matrix
does \strong{not} depend on the unknown \eqn{\boldsymbol{p}} at fit time.
Do not copy it into every slice of a DeCovarT tensor: that would
yield \eqn{\|p\|_2^2\boldsymbol{\Sigma}_{\mathrm{GLS}}}, which still
depends on \eqn{p}.
}
\examples{
Sigma <- array(c(diag(2), 2 * diag(2)), dim = c(2, 2, 2))
fixed_gls_covariance(Sigma)
}
\seealso{
\code{\link[=deconvolute_ratios_gls]{deconvolute_ratios_gls()}}
}
