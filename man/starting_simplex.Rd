% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_03_DeCovarT_estimate_ratios_frequentist.R
\name{starting_simplex}
\alias{starting_simplex}
\title{Open-simplex start for ILR solvers}
\usage{
starting_simplex(
  n_celltypes,
  initial_p = NULL,
  nms = NULL,
  y = NULL,
  mean_signature_matrix = NULL,
  dirichlet_alpha = 1
)
}
\arguments{
\item{n_celltypes}{Integer \eqn{J}.}

\item{initial_p}{One of:
\itemize{
\item \code{NULL} or \code{"barycentre"}: equi-balanced
\eqn{(1/J,\ldots,1/J)};
\item a numeric vector of length \eqn{J}: used as-is after
\code{\link[=repair_simplex]{repair_simplex()}};
\item \code{"dirichlet"}: one Dirichlet\eqn{(\alpha,\ldots,\alpha)} draw
(see \code{dirichlet_alpha});
\item \code{"qp"} (aliases \code{"deconrnaseq"}, \code{"lsei"}): mean-only simplex QP
from \code{\link[=deconvolute_ratios_deconrnaseq]{deconvolute_ratios_deconrnaseq()}} (\code{y} and
\code{mean_signature_matrix} required).
}}

\item{nms}{Optional names (cell-type colnames).}

\item{y, mean_signature_matrix}{Bulk and signature, required for a QP
start.}

\item{dirichlet_alpha}{Positive concentration, recycled to length
\eqn{J}. The default \code{1} is uniform on the simplex.
\eqn{\alpha>1} concentrates mass near the barycentre;
\eqn{\alpha<1} puts extra mass near faces (boundary-biased
restarts).}
}
\value{
A length-\eqn{J} open-simplex vector.
}
\description{
The convolution log-likelihood is not globally concave, so the start
can change which basin the solver enters. Draw several independent
Dirichlet starts (different RNG streams or
\code{\link[=multistart_decovart]{multistart_decovart()}}) rather than a single draw when probing
multimodality.
}
\examples{
starting_simplex(3L)
set.seed(1)
starting_simplex(3L, "dirichlet")
}
\seealso{
\code{\link[=multistart_decovart]{multistart_decovart()}}, \code{\link[=deconvolute_ratios_deconrnaseq]{deconvolute_ratios_deconrnaseq()}}
}
