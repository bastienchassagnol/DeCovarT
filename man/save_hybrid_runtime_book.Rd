% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_05_hybrid_latent_visualisation.R
\name{save_hybrid_runtime_book}
\alias{save_hybrid_runtime_book}
\title{Solver wall-clock time (one page per composition)}
\usage{
save_hybrid_runtime_book(artefacts, file, data_rds = NULL)
}
\arguments{
\item{artefacts}{Split or assembled fig03 artefacts from
\code{\link[=read_simulation_artefacts]{read_simulation_artefacts()}}.}

\item{file}{Output PDF path.}

\item{data_rds}{Optional directory for ggplot \code{data} RDS files.}
}
\value{
\code{file}, invisibly.
}
\description{
Facets MixSim overlap; topology is on the x-axis; colour / fill /
grouping are the five fig03 solvers. y is elapsed seconds on a log10
scale, with a left-side rug of the raw Monte Carlo draws.
}
\examples{
if (requireNamespace("ggdist", quietly = TRUE)) {
  opt <- tibble::tibble(
    ID = rep("V1", 20),
    sample_id = paste0("s", 1:20),
    algorithm = rep(c("lsei", "LBFGS"), each = 10),
    elapsed_sec = runif(20, 0.01, 0.2),
    graph_ct1 = "scale_free",
    graph_ct2 = "scale_free",
    overlap_label = "low",
    proportions = "balanced"
  )
  artefacts <- list(optimisation = opt)
  tf <- withr::local_tempfile(fileext = ".pdf")
  save_hybrid_runtime_book(artefacts, tf)
}
}
\seealso{
\code{\link[=save_hybrid_memory_book]{save_hybrid_memory_book()}}, \code{\link[=plot_mc_raincloud]{plot_mc_raincloud()}}
}
