% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_02_generate_synthetic_networks.R
\name{simulate_hierarchical_grn_moments}
\alias{simulate_hierarchical_grn_moments}
\title{Simulate GRN first- and second-order moments}
\usage{
simulate_hierarchical_grn_moments(
  n_genes,
  n_celltypes = 2L,
  mean_scale = 10,
  target_cosine = 0,
  precision_shift,
  precision_scale,
  prop_inhibitory = 0.5,
  graph_model = c("erdos_renyi", "hub", "star", "scale_free", "stochastic_block_model",
    "small_world"),
  graph_params = list(),
  adjacency = NULL
)
}
\arguments{
\item{n_genes}{Integer \eqn{G}; must be at least \code{n_celltypes}.}

\item{n_celltypes}{Integer \eqn{J\ge 2}.}

\item{mean_scale}{Positive scalar \eqn{s} (centroid norms). Default
\code{10}, as in the nine factorial scenarios. Hold fixed when
studying cosine / collinearity alone.}

\item{target_cosine}{Numeric in \eqn{[0,1]}, the collinearity dial
\eqn{\rho}.}

\item{precision_shift}{Diagonal cushion \eqn{u} for the spectral
shift (scalar, or length \eqn{J}).}

\item{precision_scale}{Positive magnitude \eqn{v} of signed
off-diagonal precision weights (scalar, or length \eqn{J}).}

\item{prop_inhibitory}{Numeric in \eqn{[0,1]}; fraction of edges with
positive precision weight (inhibitory partial correlation). Default
\code{0.5} balances inhibitory and activatory edges (scalar, or
length \eqn{J}).}

\item{graph_model}{One of \code{"erdos_renyi"}, \code{"hub"},
\code{"scale_free"}, \code{"stochastic_block_model"},
\code{"small_world"}, or a character vector of length \eqn{J}.}

\item{graph_params}{Named list of generator parameters (shared), or a
list of length \eqn{J} of such named lists (see
\code{generate_random_network_skeleton()}).}

\item{adjacency}{Optional pre-built undirected adjacencies: a list of
\eqn{J} \eqn{G\times G} matrices, or a \eqn{G\times G\times J}
array. When supplied, \code{graph_model} / \code{graph_params} are
ignored for skeleton generation.}
}
\value{
Named list with:
\itemize{
\item \code{mean_profiles}: matrix \eqn{\boldsymbol{\mu}};
\item \code{covariance_matrices}: array
\eqn{(\boldsymbol{\Sigma}_j)_{j}\in\mathcal{M}_{G\times G\times J}};
\item \code{precision_matrices}: array
\eqn{(\boldsymbol{\Omega}_j)_{j}\in\mathcal{M}_{G\times G\times J}};
\item \code{graph_structure}: \code{adjacency_matrices}, \code{weighted_adjacencies},
and \code{normalised_precision} (all \eqn{G\times G\times J});
\item \code{objectives}: \code{mean_abs_cosine} and \code{sum_euclidean_distance}.
}
}
\description{
Builds a mean matrix \eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}}
and \strong{cell-type-specific} second-order moments
\eqn{(\boldsymbol{\Omega}_j,\boldsymbol{\Sigma}_j=\boldsymbol{\Omega}_j^{-1})_{j=1}^{J}}
under a graph-constrained precision model. Means follow the
AutoGeneS-inspired cosine construction of
\code{generate_mean_signature_matrix()} with target pairwise cosine
\eqn{\rho}. For each cell type, an adjacency is drawn from a
random-graph model (or supplied), i.i.d. signed weights with
inhibitory fraction \code{prop_inhibitory} form \eqn{\boldsymbol{W}_j},
and the precision is completed by a spectral shift. Distinct cell
types receive \strong{independent} precision draws by default (biology
rarely shares one network across types); pass a length-\eqn{J}
\code{graph_model} / \code{graph_params} or a pre-built
\code{adjacency} list / array for hybrid designs.
}
\examples{
set.seed(42)
moments <- simulate_hierarchical_grn_moments(
  n_genes = 40L,
  n_celltypes = 3L,
  mean_scale = 10,
  target_cosine = 0.1,
  precision_shift = 0.1,
  precision_scale = 0.3,
  prop_inhibitory = 0.5,
  graph_model = "scale_free"
)
str(moments, max.level = 2)

}
