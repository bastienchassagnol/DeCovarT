% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_06_decovart_inference.R
\name{boundary_diagnostics}
\alias{boundary_diagnostics}
\title{Boundary and stationarity diagnostics for one DeCovarT fit}
\usage{
boundary_diagnostics(
  p,
  y,
  mean_signature_matrix,
  Sigma,
  boundary_tol = 1e-08,
  score_tol = 1e-04
)
}
\arguments{
\item{p}{Estimated proportions of length \eqn{J}.}

\item{y}{Numeric vector (or one-column matrix)
\eqn{\boldsymbol{y}\in\mathbb{R}^{G}}.}

\item{mean_signature_matrix}{Numeric matrix
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (plug-in means).}

\item{Sigma}{Array of cell-type covariances in
\eqn{\mathcal{M}_{G\times G\times J}}.}

\item{boundary_tol}{Threshold on \eqn{\min_j\hat{p}_j} below which the
estimate is flagged as near-boundary.}

\item{score_tol}{Threshold on the ILR score norm below which the
iterate counts as stationary.}
}
\value{
A one-row data frame of diagnostics.
}
\description{
Separates three claims that a bare optimiser return code conflates:
the solver stopped, the iterate is genuinely stationary with the right
local curvature, and the estimate sits close to a simplex face. None of
them implies global uniqueness, which the DeCovarT log-likelihood does
not have for a single observed sample (see
\code{vignette("DeCovarT-MLE-properties")}).
}
\details{
Reported fields are the ILR score norm
\eqn{\lVert\nabla_{\boldsymbol{z}}\ell\rVert}, the largest
eigenvalue \eqn{\lambda_{\max}(\mathbf{H}_{\boldsymbol{z}})} (negative
at a local maximum), \code{boundary_distance} \eqn{=\min_j\hat{p}_j}, and the
flags \code{near_boundary} and \code{local_maximum}.

\code{boundary_tol} is a \strong{statistical} warning threshold for Wald / ILR
linearisation, deliberately much larger than the machine-precision guard
that decides whether a logarithm is representable. A fit with
\eqn{\min_j\hat{p}_j\ll 1} is not evidence of optimiser failure: the
solver may be correctly approaching a genuine boundary optimum. It \emph{is}
evidence that interior Wald intervals should be replaced by the profile
or boundary-calibrated tests of \code{\link[=lrt_decovart]{lrt_decovart()}} and
\code{\link[=confint_profile_decovart]{confint_profile_decovart()}}.
}
\examples{
mu <- matrix(c(20, 40, 15, 40, 20, 25), nrow = 3)
colnames(mu) <- paste0("ct", 1:2)
Sigma <- array(c(diag(3), diag(3)), dim = c(3, 3, 2))
y <- drop(mu \%*\% c(0.6, 0.4))
boundary_diagnostics(c(0.6, 0.4), y, mu, Sigma)
}
\seealso{
\code{\link[=fit_decovart]{fit_decovart()}}, \code{\link[=lrt_decovart]{lrt_decovart()}}

Other decovart_inference: 
\code{\link{bootstrap_decovart}()},
\code{\link{chi_bar_square_pvalue}()},
\code{\link{confint_profile_decovart}()},
\code{\link{equivariance_check_decovart}()},
\code{\link{lrt_decovart}()},
\code{\link{multistart_decovart}()},
\code{\link{profile_loglik_decovart}()},
\code{\link{reference_bootstrap_decovart}()},
\code{\link{restricted_mle_decovart}()}
}
\concept{decovart_inference}
\keyword{internal}
