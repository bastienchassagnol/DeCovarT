% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_06_decovart_inference.R
\name{equivariance_check_decovart}
\alias{equivariance_check_decovart}
\title{Permutation-equivariance check for a labelled reference}
\usage{
equivariance_check_decovart(
  y = NULL,
  mean_signature_matrix,
  Sigma,
  bulk_expression = NULL,
  perm = NULL,
  epsilon = 10^-8,
  itmax = 500L
)
}
\arguments{
\item{y}{Numeric vector (or one-column matrix)
\eqn{\boldsymbol{y}\in\mathbb{R}^{G}}.}

\item{mean_signature_matrix}{Numeric matrix
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (plug-in means).}

\item{Sigma}{Array of cell-type covariances in
\eqn{\mathcal{M}_{G\times G\times J}}.}

\item{bulk_expression}{Numeric matrix \eqn{\boldsymbol{Y}\in
  \mathcal{M}_{G\times N}} of replicate bulk profiles sharing one
composition, used instead of \code{y} for the pooled (population) test.}

\item{perm}{Integer permutation of \code{1:J}. Defaults to reversing the
column order so the check is deterministic.}

\item{epsilon, itmax}{Relative convergence tolerance and iteration
budget passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A list with \code{perm}, \code{p_hat}, \code{p_star} (fit on the relabelled
reference), \code{p_expected} (\eqn{\hat{\boldsymbol{p}}} reordered by
\code{perm}), \code{max_abs_diff}, and \code{loglik_diff}.
}
\description{
Software check, not a bootstrap and not a test of the
bulk-to-signature match. In a \emph{reference-based} model the cell-type
names are anchored by the signature columns, so shuffling those names
(or the gene axis) is not a valid uncertainty procedure. What \emph{is}
required is that the estimator be equivariant: if
\eqn{\boldsymbol{\mu}^{\star}=\boldsymbol{\mu}Q} for a permutation
matrix \eqn{Q}, the fitted composition should satisfy
\eqn{\hat{\boldsymbol{p}}^{\star}\approx Q^{\top}\hat{\boldsymbol{p}}}
and the reconstructed bulk should be unchanged.
}
\details{
Columns of \eqn{\boldsymbol{\mu}} and matching slices of
\eqn{\boldsymbol{\Sigma}_j} are reordered by \code{perm} while the \emph{names}
stay in the original order, matching the convention of
\code{vignette("DeCovarT-MLE-properties")}. The labelled MLE on the
relabelled reference is then compared with
\eqn{\hat{\boldsymbol{p}}_{\mathrm{perm}}}.

Use \code{\link[=reference_bootstrap_decovart]{reference_bootstrap_decovart()}} for percentile intervals that
resample the experimental units of the reference (donors or cells)
or redraw compositions from a Dirichlet law.
}
\examples{
mu <- matrix(c(20, 40, 15, 40, 20, 25), nrow = 3)
colnames(mu) <- paste0("ct", 1:2)
Sigma <- array(c(diag(3), diag(3)), dim = c(3, 3, 2))
y <- drop(mu \%*\% c(0.7, 0.3))
equivariance_check_decovart(y, mu, Sigma)$max_abs_diff
}
\seealso{
\code{\link[=reference_bootstrap_decovart]{reference_bootstrap_decovart()}}, \code{\link[=loglik_multivariate]{loglik_multivariate()}}

Other decovart_inference: 
\code{\link{bootstrap_decovart}()},
\code{\link{boundary_diagnostics}()},
\code{\link{chi_bar_square_pvalue}()},
\code{\link{confint_profile_decovart}()},
\code{\link{lrt_decovart}()},
\code{\link{multistart_decovart}()},
\code{\link{profile_loglik_decovart}()},
\code{\link{reference_bootstrap_decovart}()},
\code{\link{restricted_mle_decovart}()}
}
\concept{decovart_inference}
\keyword{internal}
