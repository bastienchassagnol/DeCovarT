% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_01_run_benchmark.R
\name{check_true_theta}
\alias{check_true_theta}
\title{Validate generative-model parameters \eqn{\theta}}
\usage{
check_true_theta(
  true_theta,
  require_p = TRUE,
  J = NULL,
  second_moment = c("either", "sigma", "Theta")
)
}
\arguments{
\item{true_theta}{Named list with the fields above.}

\item{require_p}{Logical; if \code{TRUE} (default), \code{p} must be present and
valid. If \code{FALSE}, a missing \code{p} is allowed (callers may default it).}

\item{J}{Optional expected number of cell types. When \code{NULL}, inferred
from \code{mu} / the third dimension of \code{sigma} or \code{Theta}.}

\item{second_moment}{Which second-moment field to require:
\code{"sigma"}, \code{"Theta"}, or \code{"either"} (default: at least one of
\code{sigma} / \code{Theta}).}
}
\value{
\code{TRUE} invisibly if the structure is valid; otherwise stops
with an informative error.
}
\description{
Checks that \code{true_theta} stores the DeCovarT mixture parameters:
\itemize{
\item \code{p}: mixing proportions of length \eqn{J}, or a matrix
\eqn{J\times N} of sample-wise proportions (each column on the simplex);
\item \code{mu}: mean signature \eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}}
(also accepts \eqn{J\times G});
\item \code{sigma} and/or \code{Theta}: array
\eqn{G\times G\times J} of covariances and/or precision matrices.
}
}
\examples{
theta <- list(
  p = c(0.5, 0.5),
  mu = cbind(c(0, 0), c(3, 0)),
  sigma = array(c(diag(2), diag(2)), dim = c(2, 2, 2))
)
check_true_theta(theta)
}
\seealso{
\code{\link[=compute_average_overlap]{compute_average_overlap()}}, \code{\link[=compute_average_jeffreys]{compute_average_jeffreys()}}
}
