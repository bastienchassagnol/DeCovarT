% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_05_decovart_fit.R
\name{fit_decovart}
\alias{fit_decovart}
\alias{print.decovart_fit}
\alias{summary.decovart_fit}
\alias{print.summary.decovart_fit}
\alias{coef.decovart_fit}
\alias{fitted.decovart_fit}
\alias{residuals.decovart_fit}
\alias{vcov.decovart_fit}
\alias{nobs.decovart_fit}
\alias{confint.decovart_fit}
\alias{plot.decovart_fit}
\title{Fit the DeCovarT Gaussian-convolution model}
\usage{
fit_decovart(
  signature_matrix,
  bulk_expression,
  true_ratios = NULL,
  Sigma = NULL,
  method = c("Marquardt-Levenberg", "L-BFGS-B", "Newton-Raphson"),
  epsilon = 10^-4,
  itmax = 200,
  standardise = FALSE,
  scaled = FALSE
)

\method{print}{decovart_fit}(x, ...)

\method{summary}{decovart_fit}(object, ...)

\method{print}{summary.decovart_fit}(x, ...)

\method{coef}{decovart_fit}(object, ...)

\method{fitted}{decovart_fit}(object, ...)

\method{residuals}{decovart_fit}(object, ...)

\method{vcov}{decovart_fit}(object, ...)

\method{nobs}{decovart_fit}(object, ...)

\method{confint}{decovart_fit}(object, parm, level = 0.95, ...)

\method{plot}{decovart_fit}(x, ...)
}
\arguments{
\item{signature_matrix}{Mean signature
\eqn{\boldsymbol{\mu}\in\mathcal{M}_{G\times J}} (rownames = genes,
colnames = cell types).}

\item{bulk_expression}{Bulk matrix
\eqn{\boldsymbol{Y}\in\mathcal{M}_{G\times N}} (rownames = genes,
colnames = samples).}

\item{true_ratios}{Optional ground-truth proportions (\eqn{J} vector or
\eqn{J\times N} matrix). Stored on the fit; not used by the MLE.}

\item{Sigma}{Array
\eqn{(\boldsymbol{\Sigma}_j)_{j}\in\mathcal{M}_{G\times G\times J}} of
cell-type covariances.}

\item{method}{Optimiser; one of \code{"Marquardt-Levenberg"},
\code{"L-BFGS-B"}, \code{"Newton-Raphson"} (case-insensitive). These three
maps already land on the simplex (ALR or \eqn{p/\sum p}); they do
\strong{not} call \code{\link[=repair_simplex]{repair_simplex()}}.}

\item{epsilon, itmax}{Absolute convergence tolerance and iteration
budget, in the same roles as \code{reltol} / \code{maxit} in
\code{\link[stats:optim]{stats::optim()}}.}

\item{standardise}{If \code{TRUE}, apply a \strong{gene-wise} affine z-score
computed once from \eqn{\boldsymbol{\mu}} to bulk, means and covariances
(see Details). Cell-type-wise or sample-wise transforms are not
supported.}

\item{scaled}{Deprecated. \code{TRUE} (log2 mixing) always errors.}

\item{x, object}{A \code{decovart_fit}.}

\item{...}{Passed to \code{\link[graphics:plot.default]{graphics::plot()}} (plot method) or ignored.}

\item{parm}{Kept for compatibility with \code{\link[stats:confint]{stats::confint()}}; all
simplex coordinates are always returned (subsetting by \code{parm} is
not implemented). Removing this formal would break the S3 method
contract with the generic.}

\item{level}{Confidence level \eqn{1-\alpha} (default \code{0.95}). Wald
intervals use asymptotic normality of the MLE with standard errors
from \code{\link[=vcov_alr_delta]{vcov_alr_delta()}} /
\code{\link[=expected_fisher_unconstrained]{expected_fisher_unconstrained()}} (see Details of \code{\link[=fit_decovart]{fit_decovart()}}
and of \code{\link[=vcov_alr_delta]{vcov_alr_delta()}}):
\eqn{\hat{p}_j \pm z_{1-\alpha/2}\,\mathrm{SE}_j}.}
}
\value{
An object of class \code{decovart_fit}.
}
\description{
Maximum-likelihood estimator of cellular proportions
\eqn{\boldsymbol{p}_{\cdot i}} under the multivariate Gaussian convolution
\deqn{
  \boldsymbol{y}_{\cdot i}\mid(\boldsymbol{\mu},\boldsymbol{\Sigma}_j,
  \boldsymbol{p}_{\cdot i})
  \sim
  \mathcal{N}_{G}\bigl(
    \boldsymbol{\mu}\boldsymbol{p}_{\cdot i},\;
    \boldsymbol{\Sigma}(\boldsymbol{p}_{\cdot i})
  \bigr),
  \qquad
  \boldsymbol{\Sigma}(\boldsymbol{p})
  =\sum_{j=1}^{J}p_j^{2}\boldsymbol{\Sigma}_j.
}
This is a \strong{variance / likelihood specification}, not ordinary least
squares: cell-type covariances enter the residual law and cannot be
written as extra columns of \eqn{\boldsymbol{\mu}}. There is therefore no
\code{formula} / \code{lm} interface, and no \code{predict()} method for forecasting
bulk expression.

The returned object is an S3 class \code{decovart_fit} with accessors
\code{\link[=coef]{coef()}}, \code{\link[=fitted]{fitted()}}, \code{\link[=residuals]{residuals()}}, \code{\link[=vcov]{vcov()}}, \code{\link[=nobs]{nobs()}}, \code{\link[=confint]{confint()}},
\code{\link[=print]{print()}}, \code{\link[=summary]{summary()}} and \code{\link[=plot]{plot()}}. Residuals are
\eqn{\boldsymbol{Y}-\boldsymbol{\mu}\hat{\boldsymbol{P}}}; they are \strong{not} OLS
residuals. Goodness of fit is the convolution log-likelihood, not
residual sum of squares.
}
\details{
\strong{Standardisation.} CIBERSORT requires non-negative expression, no
missing values, and a non-log linear scale
\insertCite{newmanRobustEnumerationCell2015}{DeCovarT}. A logarithm is
concave, so Jensen's inequality shifts first and second moments and
\eqn{\log(\boldsymbol{\mu}\boldsymbol{p})\neq(\log\boldsymbol{\mu})\boldsymbol{p}}.
A gene-wise affine map
\eqn{\boldsymbol{x}\mapsto D^{-1}(\boldsymbol{x}-\boldsymbol{m})} with the
\strong{same} \eqn{D,\boldsymbol{m}} on \eqn{\boldsymbol{Y}}, \eqn{\boldsymbol{\mu}} and
\eqn{\boldsymbol{\Sigma}_j^\star=D^{-1}\boldsymbol{\Sigma}_j D^{-1}} leaves the
MLE of \eqn{\boldsymbol{p}} unchanged (equivariance).

\strong{Wald covariance.} Let
\eqn{\boldsymbol{\Theta}(\boldsymbol{p})=\boldsymbol{\Sigma}(\boldsymbol{p})^{-1}}.
The expected Fisher information of the unconstrained mean--covariance
map (multivariate normal; see e.g. the Wikipedia entry \emph{Fisher
information}, multivariate normal) is
\deqn{
  I(\boldsymbol{p})_{jk}
  =
  \boldsymbol{\mu}_{\cdot j}^{\top}
  \boldsymbol{\Theta}(\boldsymbol{p})
  \boldsymbol{\mu}_{\cdot k}
  +
  2 p_j p_k\,
  \mathrm{tr}\bigl(
    \boldsymbol{\Theta}(\boldsymbol{p})\boldsymbol{\Sigma}_j
    \boldsymbol{\Theta}(\boldsymbol{p})\boldsymbol{\Sigma}_k
  \bigr).
}
Cramer--Rao gives
\eqn{\mathrm{Var}(\hat{\boldsymbol{\rho}})\succeq I_{\boldsymbol{\rho}}^{-1}}
in ALR coordinates, with
\eqn{I_{\boldsymbol{\rho}}
=\mathbf{J}_{\boldsymbol{\psi}}^{\top} I(\boldsymbol{p})
\mathbf{J}_{\boldsymbol{\psi}}} and
\eqn{\mathbf{J}_{\boldsymbol{\psi}}
=\partial\boldsymbol{\psi}/\partial\boldsymbol{\rho}^{\top}}
(\code{\link[=jacobian_additive_logistic]{jacobian_additive_logistic()}}). The delta method maps the bound
back to the simplex:
\deqn{
  \mathrm{Var}(\hat{\boldsymbol{p}})
  =
  \mathbf{J}_{\boldsymbol{\psi}}
  I_{\boldsymbol{\rho}}^{-1}
  \mathbf{J}_{\boldsymbol{\psi}}^{\top}.
}
}
\examples{
toy <- readRDS(system.file(
  "extdata", "toy_deconvolution.rds",
  package = "DeCovarT"
))
fit <- fit_decovart(
  signature_matrix = toy$signature_matrix,
  bulk_expression = toy$bulk_expression,
  Sigma = toy$Sigma,
  itmax = 40
)
coef(fit)
nobs(fit)
}
\references{
\insertRef{newmanRobustEnumerationCell2015}{DeCovarT}
}
\seealso{
\code{\link[=deconvolute_ratios]{deconvolute_ratios()}},
\code{\link[=deconvolute_ratios_Marquardt_Levenberg]{deconvolute_ratios_Marquardt_Levenberg()}},
\code{\link[=expected_fisher_unconstrained]{expected_fisher_unconstrained()}}, \code{\link[=vcov_alr_delta]{vcov_alr_delta()}},
\code{\link[=coef.decovart_fit]{coef.decovart_fit()}}, \code{\link[=vcov.decovart_fit]{vcov.decovart_fit()}},
\code{\link[=confint.decovart_fit]{confint.decovart_fit()}}
}
\concept{decovart_fit}
